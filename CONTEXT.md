# CONTEXT.md — プロジェクト設計の全体像

最終更新: 2026-05-22

哲学的中核は `DESIGN_PHILOSOPHY.md`、現状と次のタスクは `HANDOFF.md` を参照。
このドキュメントは**設計の全体像**を示します。

---

## 1. プロジェクト概要

### 究極目標

**究極の省エネ・省リソース・高機能 AI を、生物の脳を模倣することで実現する。**

具体的には、Renesas DRP (Dynamically Reconfigurable Processor) などの動的再構成ハードウェア上で動作する、自律成長型 1 ビット Spiking Neural Network。

LLM・勾配降下とは原理的に切り離した設計。生物の脳の物理プロセスを単純化・近似化したものをそのまま実装する。

### 核心アイデア

**「学習」と「DRP の動的再構成」を物理的に同一視する。**

通常の DNN を DRP に乗せるのではなく、DRP の再構成機能そのものを学習機構として活用する。シナプスの open/close (= 結線の生成・刈り取り) が、DRP のスイッチ ON/OFF と直接対応する。

---

## 2. 設計哲学 (概要)

詳細は DESIGN_PHILOSOPHY.md。ここでは要点のみ。

### 6 つの設計原理

1. **局所性**: 各要素は接続された相手としか情報交換しない
2. **物理性**: 判断機構ではなく物理プロセスで構成
3. **決定論性**: 確率や乱数を使わない
4. **整数演算**: 浮動小数点を使わない
5. **創発性**: トップダウン設計せず、局所機構から振る舞いを立ち上げる
6. **平衡としての学習**: 学習用の特別な関数を持たない

### 学習の二分類

- **タイプ 1 (自己学習)**: 環境との物理的相互作用による動的平衡
- **タイプ 2 (教示学習)**: 人類の文明的標準を獲得 (教師の介入)

両者の機構は同じ (ヘブ則、STDP)。違うのは入力構造のみ。

---

## 3. 階層アーキテクチャ (12 モジュール構成)

人間の脳をベースにした分業構造。各モジュールは固有の責務を持ち、それを超えない。

```
[外部世界]
    ↓ 音 / 光 / 接触
[M0 蝸牛]    [将来: M0' 網膜、M0'' 皮膚]
    ↓
[M1 A1] ──────── ボトムアップ
    ↓                    ↑ フィードバック
[M2 A2] ←───────────────┐
    ↓                    │
[M3 カテゴリ]            │
    ↓                    │
┌──[M8 視床]──────────[M9 NA, M10 ACh]
│      ↓                  ↑
│   [M4 海馬]              │ (制御信号)
│      ↓                  │
│   [M5 ドーパミン] ────┐ │
│      ↓                ↓ │
│   [M6 運動計画]────[M11 小脳]
│      ↓                  │
│   [M7 運動野]────────────┘
│      ↓
│  [アクチュエータ]
│      ↓
└──[外部世界の変化] → 報酬 → M5へ
```

### 各モジュールの責務

| モジュール | 責務 | 学習タイプ | 現状 |
|---|---|---|---|
| M0 蝸牛 | 音→スパイク変換 (Gammatone) | なし (固定) | 未実装 |
| M1 一次聴覚野 (A1) | 時空間パターン → フィンガープリント | タイプ 1 | 開発中 |
| M2 二次聴覚野 (A2) | 不変表現の獲得 | タイプ 1 | 未着手 |
| M3 聴覚カテゴリ野 | 離散カテゴリ形成 | タイプ 1 | 未着手 |
| M4 海馬 | 短期エピソード記憶 | タイプ 1 | 未着手 |
| M5 報酬系 | ドーパミン模擬、報酬予測 | タイプ 1 + 2 | 未着手 |
| M6 運動計画野 | 行動選択 | タイプ 2 | 未着手 |
| M7 運動野 | 筋肉制御 | タイプ 2 | 未着手 |
| M8 視床 | 中継・ゲーティング | 制御系 | 未着手 |
| M9 ノルアドレナリン系 | 覚醒・学習率調整 | 制御系 | 未着手 |
| M10 アセチルコリン系 | 注意 | 制御系 | 未着手 |
| M11 小脳 | 運動精密化 | タイプ 1 (誤差学習) | 未着手 |

### 重要な責務分離

- **target/reward は M5 の責務**: M1 に入れない
- **不変性は M2 の責務**: M1 は連続性だけ提供
- **意味・カテゴリは M3 以降の責務**: M1 は意味を持たない
- **タイプ 2 学習は M5 以降**: M1-M4 はタイプ 1 のみ

---

## 4. M1 の詳細設計

### 責務

**入力スパイクパターン空間から出力フィンガープリント空間への、連続的で再現性のある写像 F を実装する。**

任意の入力 X に対して、F(X) は以下を満たす:

1. **決定論性**: 同じ X に対する F(X) は安定
2. **連続性**: X に近い X' に対する F(X') は F(X) に近い
3. **識別性**: X と本質的に異なる Y に対しては F(X) ≠ F(Y)
4. **安定性**: 経験を積んでも F(X) は大きく変わらない
5. **容量**: 区別可能な入力パターンの数が十分大きい

### 動作機構

物理プロセスのみで構成 (詳細は DESIGN_PHILOSOPHY.md §6):

- **ニューロン**: LIF 型、整数 membrane と threshold、発火で readiness 消費・threshold 上昇、時間で回復
- **シナプス**: 1 ビット結線、整数 connection_strength、STDP で直接強度変化、強度が閾値超えで exists 自動更新
- **STDP**: 因果ペアで LTP、反因果で LTD、決定論的
- **Polychronization**: 軸索遅延 1-20ms により、時空間パターンに対する複数経路の同期発火

明示的な学習関数 (apply_reward, apply_self_organization 等) は存在しない。学習は時間経過に伴う物理プロセスの結果として自然に進行。

### 入力パターン仕様

- 入力ニューロン: 20 個 (将来 40 に拡大予定)
- パターン長: 100ms (音素レベルの時間スケール)
- 時間スパン上限: 約 100ms (causal_window 80ms 制約)
- 単語・フレーズ等の長時間構造は M2 以降の責務

### 出力フィンガープリント

- 出力ニューロン: 40 個 (将来 100 に拡大予定)
- 各スパイクは「タイムスタンプ」を持つ
- 各スパイクは指数減衰する「Spike Lifetime」を持つ (生物の細胞内信号の物理的緩和に対応)
- フィンガープリント = 全 spike の Lifetime の時間軸上の重ね合わせ

---

## 5. ハードウェア戦略 (3 段階)

### 段階 0 (完了): Rust + CPU でアルゴリズム検証

- 動作確認済み
- 設計の正しさを論理的に確認

### 段階 1 (現在): PyTorch + CUDA で大規模実験

- ターゲット: RTX 3060 Laptop GPU 6GB
- 10K - 300K ニューロン規模での実験
- 注: 現在は Rust 版での実験を継続中、PyTorch 版は補助

### 段階 2 (将来): FPGA でハードウェア動作確証

- Zynq UltraScale+ などを想定
- 1 ビット信号と動的再構成のハードウェア妥当性を検証

### 段階 3 (将来): Renesas DRP に移植

- RZ/V2H の DRP1 (汎用 DRP) をターゲット
- DRP-AI3 (CNN 専用) ではない理由: 動的 SNN はその枠に乗らない
- 量産可能なエッジ AI として展開

### DRP マッピング

| 生物概念 | ソフトウェア表現 | DRP ハードウェア表現 |
|---|---|---|
| ニューロン | Neuron 構造体 | PE またはスイッチ要素 |
| シナプス | Synapse 構造体 | 配線 |
| スパイク | 1 ビット信号 | 1 ビット配線信号 |
| 軸索遅延 | delay 整数 | 配線経路長 |
| 結線 open/close | exists フラグ | スイッチ ON/OFF |
| 学習 | exists の動的変更 | DRP 動的再構成 |

「学習」と「DRP の再構成」が物理的に同一視できる。これがプロジェクトの核心。

### ハードウェア固有の数値

確定済み (データシート R01DS0429EJ0130 より):
- RZ/V2H: DRP-AI = DRP0 + AI-MAC (8/80 TOPS), 汎用 DRP = DRP1
- DRP Bus 直結 SRAM 4MB
- LPDDR4 最大 16GB

未確定 (Hardware User's Manual R01UH1015 取得待ち):
- DRP1 の PE 数
- 再構成サイクル数
- プログラミングインターフェース詳細

---

## 6. 数値パラメータと根拠

### Izhikevich ニューロン (連続版、network.rs、現在は未使用)

```
a = 0.02, b = 0.2, c = -65, d = 8   # 興奮性 RS
a = 0.10, b = 0.2, c = -65, d = 2   # 抑制性 FS
```
出典: Izhikevich (2003) 標準値

### 1 ビット LIF (binary_network.rs、本命)

```
exc_threshold = 80, leak = 2, refr = 4 steps   # 興奮性
inh_threshold = 40, leak = 3, refr = 2 steps   # 抑制性
spike_gain = 10                                # シナプス1本のカウンタ寄与
```

8 coincident spikes で発火する設計。

### STDP

```
tau_plus = tau_minus = 20 ms          # Bi & Poo (1998) 実験値
A_plus = 0.10, A_minus = 0.12         # A_minus > A_plus で安定 (Song et al. 2000)
causal_window = 80 ms
```

### シミュレーション

```
dt = 0.5 ms                           # SNN 標準
trial duration = 300 ms (将来は連続)
pattern duration = 100 ms 以内
```

---

## 7. 過去の試行と学んだこと

### 13 試行の R-STDP 報酬学習実験 (主に失敗)

selectivity 0.32-0.58 で頭打ち。原因の診断:

1. **target_a/target_b の人為指定が教師なし設計を破壊**: M5 機能を M1 に押し込んでいた階層違反
2. **delay-shift staleness は有害**: 「閉じたシナプス」より「タイミングずれた open シナプス」が悪い
3. **入力強度スケーリング + wrong=-1.0 + silence=0.0 で最高 0.581**: ただし設計違反の組み合わせ
4. **ニューロン数増加は逆効果**: 単純な拡大では密度問題が出る
5. **訓練時間増加は天井を破れない**: 1k と 10k で同じ天井

### 可視化導入による発見

- trial 150-200 で大量プルーニング (構造変化)
- これは「崩壊」ではなく「閾値変化で既存判断が表面化」しただけ
- 初期 180 試行で reward が baseline を下回る (= 教師信号が初期構造を破壊)
- PCA で「シナプス分布の低次元構造」が見える

### パターン X 採用 (時間順序設計)

selectivity 天井 0.32 → 0.42 (POST) / 0.499 (PERSIST) に向上。
ただし PERSIST 崩壊と silent 過多の問題が残る。

### 慣化機構導入

ユーザー直感「同じ刺激に慣れる、時間で戻る」を実装。
POST selectivity 0.702、PERSIST 崩壊 97.4% → 15.9%。
ただし実装が判断機構を含んでおり、設計哲学に反していた。

### 設計哲学の確立 (最重要)

ユーザー指摘により以下が確定:

1. **物理プロセスのみで構成**: 判断機構を排除
2. **学習の二分類**: タイプ 1 と タイプ 2 を区別、M1 はタイプ 1 のみ
3. **連続性が M1 の核心価値**: 同一性ではなく連続性を保証
4. **DRP マッピングとの完全整合**: 1 ビット信号、整数演算、動的再構成

これにより、これまでの実装の多くを書き直す方針が確定。

---

## 8. 用語集 (重要)

このプロジェクトでは独自の用語を使う。LLM の標準用語に翻訳しない。

### Polychronization (Izhikevich 2006)

軸索遅延の組み合わせにより、特定の時空間スパイクパターンに対して複数のニューロン経路が同期発火する現象。M1 における連続性の物理的基盤。

### Spike Lifetime

出力スパイクが時間軸上で指数減衰する「残像」。生物の細胞内信号 (カルシウム濃度、神経伝達物質) の物理的緩和に対応。フィンガープリント生成の基礎。

### Fingerprint (フィンガープリント)

出力層の Spike Lifetime の時間軸上の重ね合わせベクトル。同じ入力は同じフィンガープリントへ収束する (共鳴)。M1 の出力。

### 構造的可塑性 (Structural Plasticity)

シナプスの新生・刈り取り。重みの変化ではなく、結線そのものの生成・消滅。DRP の動的再構成と物理的に対応。

### 動的平衡 (Dynamic Equilibrium)

系を構成する要素は常に変化しているが、マクロな性質は安定している状態。本プロジェクトでは「学習結果」と同義。

### タイプ 1 / タイプ 2 学習

- タイプ 1: 環境との物理的相互作用による自己学習
- タイプ 2: 人類の文明的標準を獲得する教示学習

### 1 ビット信号

各ニューロンの出力は「発火するかしないか」の 1 ビット。生物の神経細胞の物理的振る舞いと整合。DRP の配線リソース最小化に直結。

---

## 9. やってはいけないこと (まとめ)

1. **LLM ベースの「改良」をしない**: 意図的に切り離している
2. **連続重み版に戻さない**: 1 ビット版が本命
3. **target/reward を M1 に再導入しない**: M5 の責務
4. **判断機構を入れない**: target_rate 比較、累積閾値判定、確率的伝送など全て NG
5. **損失関数・目的関数を使わない**: 物理プロセスの結果として学習が起きる
6. **「あと一押し」モードでパラメータをいじらない**: 設計を見直す方が先
7. **複数の変更を同時に入れない**: 何が効いたか分からなくなる
8. **delay-shift staleness を復活させない**: 根本的に有害と判明済み
9. **Rust 版を捨てない**: DRP 移植のための参照実装

---

## 10. 参考文献

### 必読

- **Izhikevich, E.M. (2006)** "Polychronization: Computation with Spikes" *Neural Computation*
  - 軸索遅延と STDP による時空間パターン認識の理論的基盤
- **Bi, G. Q. & Poo, M. M. (1998)** STDP の実験的確立
- **Hebb (1949)** "Cells that fire together, wire together"
- **Bliss & Lømo (1973)** LTP の実験的実証

### 設計哲学に関連

- **Friston (2010)** 自由エネルギー原理 (我々の動的平衡論と関連)
- **McClelland et al. (1995)** 補完的学習システム (タイプ 1/2 区分のヒント)

### 避ける (LLM 系)

- Vaswani et al. (2017) Transformer
- Devlin et al. (2018) BERT
- Brown et al. (2020) GPT-3
- Rumelhart et al. (1986) Backpropagation

これらは興味深い研究だが、本プロジェクトの設計思想と相容れない。

---

このドキュメントは設計の全体像を示す。判断に迷ったら DESIGN_PHILOSOPHY.md に戻り、6 原理に照らすこと。
