# M1.5 皮質中継 (時相変換) 設計 — リレー核 #2

作成: 2026-08-04 / 状態: 設計検討中 (実装前、ユーザー確認待ち)
位置づけ: M1 (A1) と M2 (A2) の間の欠落リレー核。
関連: `ARCHITECTURE_REVIEW_2026-08-04.md`、`M0_5_COCHLEAR_NUCLEUS_DESIGN.md`、`M2_A2_DESIGN.md`

---

## 1. 位置づけと動機

アーキ検証 (2026-08-04) で **リレー核原理**が確立した:
階層 SNN は全境界に「上流出力を時間構造へ整え直すリレー段」を要する。
M0→M1 は M0.5 蝸牛神経核で解決したが、M1→M2 はリレー段が無く M2 が collapse している
(within 1.000 / active 5/20 / between 0.74)。本文書はその欠落リレー核 M1.5 を設計する。

**重要**: M0.5 設計文書は「蝸牛神経核を M1 出力に適用する」と記していたが、
本設計はこれを**採らない**。M1 出力の実測 (下記 §2) が、M1→M2 の問題は M0→M1 とは
**本質的に別物**であることを示したためである。

---

## 2. 決定的な実測: M1 出力の時間構造 (probe 2026-08-04)

`m1_output_probe` (訓練済み M1、best 構成 speed=3 decay_slow=30、20 sample 平均) の結果:

| 音素 | spike総数/trial | active | 初発火ms | ピーク bin | 時間集中度 |
|---|---|---|---|---|---|
| pa | 19.9 | 19/40 | 151.3 | 150ms | 0.29 |
| ki | 20.5 | 19/40 | 150.2 | 150ms | 0.38 |
| tu | 20.9 | 19/40 | 150.2 | 150ms | 0.39 |
| se | 25.6 | 20/40 | 145.5 | 150ms | 0.28 |
| mo | 20.9 | 20/40 | 152.3 | 150ms | 0.29 |

全音素平均の population 時間プロファイル (10ms bin、発火延べ数):
```
bin: 0 .............. 14  15  16  17  18  19 ....... 29
値:  0 (140msまで0)   1   7   5   4   2   0 (以降ほぼ0) 2
```

timing のみ (identity 無視) の音素間 cosine: **平均 0.954** (pa-ki 0.963, ki-tu 0.987, …)

### 2.1 実測から確定した M1 出力の性質

1. **極めて疎**: 1 trial あたり spike は ~20 本 (40 ニューロン × 600 step の中で)。
   診断の「40ch 密スパイク列」は fingerprint 積分後の見え方であり、**生出力は疎**。
2. **単一同期バースト**: 全音素が **150ms 付近で一斉に発火**し、その後は沈黙。
   持続型 (平坦) でもオンセット冒頭型 (0ms) でもない。
   150ms は decay_slow=30 で M1 が反復曝露を積分し、中盤で閾値到達する「判断の瞬間」。
3. **timing は音素非依存**: 全音素が同じ 150ms に鳴る (timing cosine 0.954)。
   **音素を分けているのは identity (どの ~7 ニューロンが鳴るか) だけ**。
4. **同時発火数**: ピークで 5.7〜8.2 個の distinct 出力が同時 (bin15)。

### 2.2 これが M2 collapse の真因

M2 は polychronization (軸索遅延 + STDP の因果窓) で入力の**時間構造**を使って分離する。
しかし M1 は全音素を**同一タイミング (150ms 同期一発)** で送るため、M2 から見ると
どの音素も**時間的に瓜二つ**。M2 の時間機構が使うべき「タイミングの差」が存在しない
→ 入力に依存しない単一アトラクタへ collapse する。

M1 出力に含まれる音素情報 (identity) は**空間コード**であり、M2 が読める**時間コード**に
なっていない。これが欠落リレー核の正体。

---

## 3. なぜ「蝸牛神経核のコピー」が効かないか (反証済み)

| 蝸牛神経核の細胞 | M1 出力に適用した場合 | 判定 |
|---|---|---|
| Octopus (広帯域オンセット検出) | 全音素が 150ms に同期発火 → 全音素で同一の一発を返す | ✗ 分化ゼロ |
| Bushy (帯域別 1:1 中継 + notch) | identity を保つだけ、新しい時間構造を生まない | ✗ 無益 |
| Stellate (隣接プール 持続 rate) | M1 出力は持続でない → プールしても情報増えず | ✗ 無益 |

蝸牛神経核は「密で時間的にのっぺりした信号から onset を**抽出**」する装置。
M1 出力には既に鋭い同期 onset があり、欠けているのは onset ではなく
**タイミングの多様性**。よって抽出ではなく**変換**が要る。

---

## 4. M1.5 の責務: 空間コード → 時間コード変換 (時相変換)

**責務**: M1 が同期して鳴らした identity 集合 {発火した出力ニューロン} を、
音素ごとに異なる**時間シーケンス**へ展開し、M2 の polychronization が分離できる
時間構造をもった出力を生成する。

これは roster の名称「皮質中継 **時相変換**」が指すもの。分解 (M0.5) の双対。

**やること**:
- 同期空間パターン (どのニューロンが 150ms に鳴ったか) → 時間パターン (到着時刻の系列)
- 音素固有の identity 差を、音素固有の timing 差へ写す

**やらないこと** (責務外、原理・階層厳守):
- 不変性の獲得 (M2 の責務)
- カテゴリ・ラベル・報酬・正解判定 (M3+/M5+)
- 学習・可塑性 (リレー核は固定。可塑性は M1/M2 が持つ)

---

## 5. 機構案: 遅延多様リレー (Delay-Diverse Relay)

### 5.1 中核アイデア

M1 の各出力ニューロン i を、**固有の軸索遅延 dᵢ** をもって M1.5 のリレー層へ配線する。
すると「150ms に集合 S が同期発火」が、M1.5 では

```
到着時刻: {150 + dᵢ : i ∈ S}
```

という**時間的に展開された系列**になる。S が音素ごとに違えば到着系列も違う
→ M2 の polychronous な経路が音素ごとに別々に共鳴 → 分離可能。

これは物理的に純粋な**配線遅延**であり、判断機構ゼロ・決定論・整数。
DRP マッピングでは「軸索遅延 = 配線経路長」に直結する (CONTEXT.md §5 と整合)。

### 5.2 遅延の割り当て (決定論的、初期化時のみ)

- 各 M1 出力 i に遅延 dᵢ を割り当てる。同期集合を最大に時間展開するには dᵢ を
  出力インデックスに対して分散させる (例: dᵢ = 初期化時の LFSR で決まる [Dmin, Dmax] の値)。
- 遅延幅は M2 の因果窓 (M2 = 320 step = 160ms) の内側に収める必要がある。
  同期バースト (~150ms) を M2 の 1 因果窓に収まる系列へ展開する。
- **可塑性なし** (遅延は固定)。乱数は初期化時のみ (原理 3 準拠)。

### 5.3 リレー層の構成 (2 案、要検討)

**案 A (最小): 遅延線 + 恒等中継**
M1.5 は M1 出力と同数 (40) のリレーニューロンを持ち、リレー r は M1 出力 i を
遅延 dᵢ で受けて中継発火する (Bushy 的 1:1、ただし遅延付き)。
identity はそのまま、timing だけ dᵢ で展開。最もシンプル。M2 入力 40ch 維持。

**案 B (時相統合): 遅延 + 同時性再検出**
案 A の遅延線の後に、複数の遅延到着を積分して「特定の到着系列」に選択的に発火する
検出ニューロン層を置く (Octopus 的だが対象は M1 出力の identity 系列)。
音素固有の到着系列 → 固有の検出ニューロンが発火 → M2 へより明瞭な時間パターン。
表現力は高いが調整が難しい。

**推奨**: まず案 A で「遅延展開だけで M2 collapse が緩むか」を検証 (最小介入、
何が効いたか切り分け可能 — 原理: 複数の変更を同時に入れない)。緩めば案 B で強化。

### 5.4 onset アンカー (補助、任意)

M1 出力の 150ms 同期 onset は「M1 が判断した瞬間」の明瞭な時刻マーカーでもある。
これを Octopus 相当の 1 細胞で検出し、M2 の trial 内時間基準 (t=0 リセット) として
使う選択肢がある。案 A では必須でないが、案 B の到着系列を「アンカーからの相対時刻」で
測ると安定する可能性。初期実装では省略し、必要なら追加。

---

## 6. 期待される機能と成功基準

### 6.1 期待機能

M1.5 を挟むと:
1. M1.5 出力の timing-only 音素間 cosine が 0.954 から**低下** (timing が音素を運ぶようになる)
2. M2 出力の within が 1.000 から下がり (固定点ロック解除)、active が 5/20 から増加
3. M2 per-pair between が M1 の 0.765 と同等以下 (少なくとも collapse しない)

### 6.2 成功基準

| 指標 | 現状 (M1→M2 直結) | 期待 (M1.5 経由) |
|---|---|---|
| M1.5 出力 timing-only cosine | 0.954 (M1 出力) | < 0.85 |
| M2 within | 1.000 (固定点ロック) | < 0.95 |
| M2 active | 5/20 | 増加 (> 10/20) |
| M2 per-pair between | collapse (~1.0 相当) | < 0.85 |

### 6.3 反証基準 (falsification)

案 A (遅延展開のみ) で M1.5 出力の timing-only cosine が下がらない、または M2 within が
1.000 のまま下がらないなら、「遅延多様だけでは同期バーストを分離可能な時間構造に
できない」= 仮説の見直しが必要。次に案 B、それでも駄目なら M1 出力自体を
(同期一発でなく) 時間展開させる方向 (M1 の decay/発火動態の見直し) へ遡る。

### 6.4 案 A 検証結果 (2026-08-04): 反証 → 案 B へ

`relay_probe` (訓練 3000、seed 43981、遅延 min2/max58/mean27.9 step) の結果:

| 段 | timing-only 音素間 cosine (2ms bin) |
|---|---|
| M1 出力 | 0.864 |
| M1.5 出力 (案 A) | 0.873 (**Δ +0.009、改善せず**) |

**構造的診断**: 固定の per-channel 遅延は**ほぼ可逆な変換**であり、音素間の cosine 構造を
保存するため分離度を上げられない。さらに M2 は自前の入力遅延 (delay_range 2-40) を持つ
ため、M1.5 の固定遅延は M2 の配線付け替えとほぼ等価で、M2 が学習で吸収できてしまう。
→ **案 A (純粋な遅延多様 1:1 リレー) は機構として不十分**と判定。

**再認識**: M0.5 蝸牛神経核が効いたのは「遅延」ではなく、Octopus の**非線形な同時性
しきい値**が蝸牛出力に無かった**新しいチャネル**を創出したこと。リレー核の本質は
「既存信号を遅らせる」ではなく「**非線形演算で新しい派生特徴を創る**」ことにある。

**方向**: 案 B (遅延線 + 同時性再検出) へ。到着系列の**物理的同時性**に発火する検出
ニューロン (M1 出力版 Octopus) が、音素サブセット選択の派生チャネルを創れるか検証する。
判断機構ではなく閾値同時性 (原理 2)、ラベルなしで創発 (原理 5) を保つ。

### 6.5 案 B 検証結果 (2026-08-04): 部分成功だが M2 collapse は解けず

5 段パイプライン `m0_cn_m1_relay_m2_pipeline` (2000 訓練、baseline vs coinc) の POST:

| 段 | baseline (素通し) | 案 B (同時性検出) |
|---|---|---|
| M1.5 per-pair | 0.410 (= M1) | **0.385** (M1 より分化↑) |
| M2 within | 0.992 | 0.911 |
| M2 active | 5/20 | 5/20 |
| M2 per-pair | 0.697 | 0.681 |

**読み解き**:
1. 案 B は狙い通り**新しい分離を創った** (M1.5 per-pair 0.410→0.385)。可逆な案 A (0 改善)
   との違いは明確で、非線形同時性検出の方向は正しい。
2. **しかし M2 collapse は解けない**。within は僅かに緩む (0.992→0.911) が active は 5/20 の
   まま、per-pair も 0.68 で高止まり。入力をどう整形しても M2 は同じ 5 ニューロンにロック。

**再フレーミング (重要)**: **M2 の collapse は「入力の分離度」ではなく「M2 自身の力学」の
問題**だった。M0.5 が M1 を救えたのは M1 の collapse が入力構造の問題だったから。M2 は
別物で、入力再符号化 (案 A/B) では動かない。M1 出力 (~20 spikes の単発バースト) は M2 を
デフォルトアトラクタから蹴り出すには弱すぎ・短すぎる。

**非対称の核心**: M0.5 が M1 に与えたのは 300ms 全体の**持続的リッチな多ストリーム**
(Stellate 持続 rate 等)。M1.5 (案 A/B とも) は依然 **150ms の単発バースト**を返すだけ。
→ 次の仮説 H2': M1.5 は identity 再符号化だけでなく、M1 の単発バーストを**持続ストリームへ
時間展開**し、M2 に十分な持続 drive を与える必要がある。あるいは M2 側の力学 (再帰・抑制・
閾値) の見直しが要る。ここは設計判断のため**ユーザーと相談**する。

### 6.6 coinc_spread 検証 + 総括 (2026-08-04): 入力再符号化は M2 collapse を解けない

案 B の同時性検出出力を per-detector 遅延で時間展開 (coinc_spread) を試した POST:
M1.5 per-pair = 0.482 (案 B 0.385 より悪化)、M2 within=0.984 / active=5/20 (素通しと同等)。
遅延展開は派生コードを smear し逆効果。

**4 機構の総括**:

| 機構 | M1.5 per-pair | M2 within | M2 active |
|---|---|---|---|
| (M1 出力) | 0.410 | — | — |
| 素通し | 0.410 | 0.992 | 5/20 |
| 案 A 遅延 | 0.410 (可逆) | — | — |
| 案 B 同時性 | 0.385 (分化↑) | 0.911 | 5/20 |
| coinc_spread | 0.482 (悪化) | 0.984 | 5/20 |

**確定した結論**: 入力をどう再符号化しても (遅延・同時性・両者) M2 は必ず 5/20 にロック
して collapse する。**M2 collapse は入力の分離度ではなく M2 自身の力学の問題**。
M2 は untrained では active 20/20 だが、訓練で STDP+vitality が 5 ニューロン勝者総取りへ
収束する (decay_slow=30 適用済でも)。

**リレー核原理への含意 (重要な補正)**: M0→M1 は入力構造の問題でリレー核 (M0.5) が効いたが、
**M1→M2 はリレー核 (入力再符号化) では解けない**。境界ごとに必要な処理は一様ではなく、
M1→M2 では M2 の力学そのもの (勝者総取りを緩める) か、M1.5 を単なる再符号化でなく
**持続 drive 生成器** (M1 の単発バーストを 300ms の持続ストリームへ、reverberation で
時間展開) にする必要がある。次の方向はユーザー判断。

### 6.7 coinc_sustain 検証 (2026-08-04): 入力側 仮説空間の完全な枯渇

持続 drive 化 (検出器発火後 300 step=150ms ラッチ) を試した POST:
M1.5 per-pair = 0.774 (ラッチで密になり分化喪失、全機構中最悪)、M2 within=0.972 /
active=5/20 (依然 collapse)。持続化は識別コードを密にし逆効果。

**5 機構の最終総括**:

| M1.5 機構 | M1.5 per-pair | M2 within | M2 active | M2 per-pair |
|---|---|---|---|---|
| なし (素通し) | 0.410 | 0.992 | 5/20 | 0.697 |
| 案 A 遅延 | 0.410 (可逆) | — | — | — |
| 案 B 同時性 | 0.385 (最良) | 0.911 | 5/20 | 0.681 |
| coinc_spread | 0.482 | 0.984 | 5/20 | 0.894 |
| coinc_sustain | 0.774 (最悪) | 0.972 | 5/20 | 0.909 |

**決定的結論**: 入力を何に変えても (再符号化・時間展開・持続化) **M2 は必ず active 5/20 に
ロック**する。M2 collapse は**完全に入力非依存** = M2 自身の力学の問題。**入力側の仮説空間は
枯渇した**。M1→M2 の壁はリレー核 (入力前処理) では原理的に越えられない。

**次に必要なこと (M2 側)**: M2 は untrained では active 20/20 だが訓練で 5/20 へ収束する。
なぜ M1 (同じ ThermoNetwork コード) は収束せず M2 は収束するのか — for_m2 と for_m1_cn_40 の
差 (output 20 vs 40、causal_window 320 vs 160、grid/抑制比) の切り分けが要る。これは M2 の
設計変更であり、パラメータ弄りに陥らないため **まず collapse 力学の診断 (なぜ 5/20 か) を
行ってから** 是正する。この方向はユーザーと合意して進む。

---

## 7. 実装ロードマップ (段階的、各段でユーザー報告)

1. **probe 済**: M1 出力の時間構造を実測 (本文書 §2) — 完了
2. `cortical_relay.rs` (M1.5) の実装: 案 A (遅延線 + 恒等中継)。ThermoNeuron ベース
   か専用軽量セルか (M0.5 と同じ設計判断) を決める。
3. 単体テスト: 同期入力集合が遅延展開されて出力されることを確認。
4. `m0_cn_m1_relay_m2_pipeline` (5 段) の実装: cochlea→CN→M1→**M1.5**→M2。
5. 短時間 (500 試行) で M1.5 出力 timing-only cosine と M2 within を確認。
6. 判定: 案 A で緩むか。緩めば 5K フル評価、駄目なら案 B。
7. ki-tu への効果を per-pair で確認 (M1.5 が両 Plosive を分離できるか)。

---

## 8. 設計哲学 6 原理との整合

1. **局所性**: M1.5 は M1 出力とのみ接続、M2 へのみ投射。境界局所。✓
2. **物理性**: 軸索遅延 (配線長) のみ。判断機構なし。✓
3. **決定論性**: 遅延は初期化時に決定し以後固定。乱数は初期化のみ。✓
4. **整数演算**: 遅延は整数 step、ThermoNeuron は整数。✓
5. **創発性**: 遅延展開という単純機構から、M2 の分離能力が創発することを狙う。✓
6. **平衡としての学習**: M1.5 自身は学習しない (固定リレー)。学習は M2 の STDP が担う。✓

リレー核は**固定の物理装置**であり、蝸牛・蝸牛神経核と同じくタイプなし (可塑性なし)。
この役割分担 (リレー=前処理、皮質野=可塑性) がリレー核原理の核心。

---

## 9. 未決事項 (ユーザー確認点)

1. **機構の方向**: 「空間→時間 変換 (遅延多様リレー)」で進めてよいか。
   naive copy (蝸牛神経核) を採らない判断の確認。
2. **案 A / 案 B**: まず案 A (最小介入) から、で合意するか。
3. **セル実装**: ThermoNeuron 統一 (哲学的) か 専用軽量セル (実用的) か。
4. **遅延幅**: M2 因果窓 (160ms) 内での遅延分散幅の初期値。
5. **onset アンカー**: 初期実装で省略してよいか。
