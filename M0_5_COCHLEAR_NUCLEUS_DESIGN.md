# M0.5 蝸牛神経核 (Cochlear Nucleus) 設計ドキュメント

作成日: 2026-05-28
ステータス: 設計検討中 (実装前)
位置づけ: M0 (蝸牛/有毛細胞) と M1 (A1) の間に欠落していた信号分解ステージ

---

## 第 1 部: 一連の診断から導き出された仮説

### 1.1 診断チェーン (2026-05-27〜28)

M2 統合の不調を起点に、 原因を辿った連鎖:

```
[観察 1] M2 を足しても selectivity 改善せず (A 案 集約も B 案 内部収束も M2 sel ≈ 0.26)
   ↓ なぜ?
[観察 2] M2 の入力源である M1 自体が分化していない (M1 phoneme sel 0.389、 between 0.600)
   ↓ M1 が悪い? → 検証
[観察 3] M1 単体 + 固定パターンなら sel 0.728、 between 0.173 (健全に分化)
   ↓ M1 は健全。 では入力が悪い? → 検証
[観察 4] M0 蝸牛は音素を分化している (音素間 between 0.30、 mo 除く)
   ↓ 上流も下流も健全なのに、 なぜ?
[観察 5] per-pair 分析: M1 平均出力は全音素ペアで between ≈ 0.956〜1.000
         (pa-tu 入力 0.11 [最 distinct] でも M1 出力 1.000)
   ↓ 決定的
[結論] M1 は音素入力にほぼ反応せず、 入力非依存の「デフォルトアトラクタ」 に落ちる
```

### 1.2 中心仮説

> **M1 が音素を分化できないのは、 入力 (現状の蝸牛出力) が M1 をデフォルトアトラクタから引き離すだけの「時間構造」 を持たないため。**

補強する観察:
- 固定パターン (各入力が特定時刻に 1 回発火 = **疎で時間的に鋭い**) → M1 は分化 (sel 0.728)
- 蝸牛音素 (各帯域が持続的に発火し続ける = **密で時間的に鈍い**) → M1 は分化せず (sel 0.389)
- M1 の STDP は時間順序 (因果窓 80ms) で学習するため、 **疎で時間構造のある入力**を必要とする
- PAPER §5.11.7 (測定対象も入力統計に依存)、 §5.12.7-A (UP/DOWN が dense 入力で崩壊) と整合

### 1.3 生物学的な欠落の特定

聴覚経路と現状モデルの対応:

```
蝸牛 (有毛細胞)         → 我々の M0 ✓ (20 帯域、 biquad + 包絡 + 閾値発火)
蝸牛神経核 (3 細胞型)   → 【欠落】 ← 信号分解ステージ
上オリーブ核・下丘・MGB → 抽象化 (省略)
A1                      → 我々の M1 ✓ (Fork F-G1-R1 v2)
```

生物は蝸牛と A1 の間に **蝸牛神経核** を挟み、 3 種類の形態的に異なる細胞で音を **時間・周波数・オンセット** の並列ストリームに分解している。 現状の M0 は「包絡 rate (持続的)」 しか出力しておらず、 **オンセット標識と細かい時間構造が欠落**。

### 1.4 仮説の予測

> **3 細胞型 (Octopus / Bushy / Stellate) で蝸牛出力を 3 ストリームに分解し M1 に与えれば、 音素ごとに異なる時間構造が M1 を別アトラクタへ蹴り分け、 between が下がり selectivity が上がる。**

検証指標:
- M1 出力 per-pair between が 1.0 から低下 (特に pa-tu など distinct 音素が分離)
- POST selectivity が 0.389 から向上

---

## 第 2 部: 蝸牛神経核の生物学 (3 主要細胞型)

腹側蝸牛神経核 (VCN) の主要投射細胞。 形態が機能を決める「個性派ニューロン」。

| 細胞 | 形態 | 膜特性 | 機能 | 出力する符号 |
|---|---|---|---|---|
| **Octopus (タコ)** | 片側から太い触手状突起が多数、 多くの聴神経を横断 | 極低入力抵抗、 超高速、 強い適応 | **広帯域オンセット/同時性検出** | 「いつ音が始まったか」 (鋭い広帯域時刻) |
| **Bushy (房状)** | 短くモジャモジャした密突起、 endbulb of Held 受容 | 高速、 secure relay | **時間精度の保存 (phase-locking)** | 周波数別の「細かい時間構造/立ち上がり」 |
| **Stellate (星状/T-multipolar)** | 放射状全方位突起、 周波数横断統合 | 中速、 規則発火 (chopper) | **スペクトル包絡符号化** | 周波数別の「強度 (持続 rate)」 |

主要文献:
- Oertel D. et al. (2000) "Detection of synchrony in the activity of auditory nerve fibers by octopus cells" PNAS 97:11773 — Octopus = 同時性検出
- Young E.D., Oertel D. (2004) "Cochlear nucleus" in *The Synaptic Organization of the Brain* — 3 細胞型の機能
- Rhode W.S., Greenberg S. (1992) "Physiology of the cochlear nuclei" — chopper/onset/primary-like 応答型

---

## 第 3 部: 各細胞型の実装仕様

### 3.0 共通設計方針

- **入力**: M0 蝸牛の 20 帯域スパイク列 (各 ch: 発火で 60、 他 0、 不応期 4 step)
- **物理プロセス**: 各細胞は膜特性 (積分時定数 + 閾値 + 適応) の異なる LIF 様セル
- **整数演算のみ**、 決定論的、 判断機構なし (原理 1-6 遵守)
- **出力**: M1 入力ニューロンへの電流ベクトル

### 3.1 Octopus 細胞 (広帯域オンセット検出)

**計算**: 全 20 帯域の同時発火数を見て、 「多数の帯域が同時に立ち上がった瞬間」 (= 音のオンセット) を検出。

**実装パラメータ (提案)**:
```
N_OCTOPUS = 4              // オンセット感度の異なる 4 細胞
// step ごとに発火帯域数を数える: coincidence = Σ (ch fired ? 1 : 0)
// 各 octopus 細胞 k は閾値 COINCIDENCE_TH[k] を持つ
COINCIDENCE_TH = [3, 5, 8, 12]   // 3 帯域同時〜12 帯域同時で段階的に発火
// 適応 (オンセットのみ発火、 持続には反応しない):
OCTOPUS_REFRACTORY = 30 step (15ms)  // 一度発火したら 15ms 沈黙
// 立ち上がり検出 (持続的な高 coincidence では再発火しない):
//   coincidence が前 step より増加 (rising edge) のときのみ発火
```

**生物対応**: 低入力抵抗 = 多数同時入力が必要 (COINCIDENCE_TH)、 高速 = 即座のオンセット、 強適応 = refractory。

**出力**: 4 ch (音素ごとに異なるオンセットパターン)。

### 3.2 Bushy 細胞 (周波数別 時間精度保存)

**計算**: 各帯域を 1:1 で中継するが、 **立ち上がり (transient) を強調し持続を抑制** (適応)。 「その帯域がいつ活動を始めたか」 を鋭く伝える。

**実装パラメータ (提案)**:
```
N_BUSHY = 20              // 各帯域に 1 つ
// 各 ch の最近活動を leaky に追跡: activity[ch] += fired ? GAIN : 0; activity[ch] -= LEAK
// bushy[ch] が発火する条件: その ch が「静→動」 に転じた瞬間
//   prev_fired[ch]==false && now_fired[ch]==true → bushy 発火
// または activity 微分 (立ち上がり) ベース
BUSHY_ADAPT = 適応強め    // 持続発火する帯域では bushy 出力が減衰
```

**生物対応**: endbulb of Held = secure relay (1:1)、 ただし強い frequency-specific adaptation で transient 強調。

**出力**: 20 ch (各帯域の鋭い立ち上がり標識)。

### 3.3 Stellate 細胞 (スペクトル包絡 rate)

**計算**: 各帯域 (+ 隣接帯域プール) のスパイクを積分し、 強度に比例した規則発火 (chopper)。 現状の蝸牛出力に近い「持続 rate」。

**実装パラメータ (提案)**:
```
N_STELLATE = 20           // 各帯域に 1 つ
// 隣接 ±1 帯域をプール (周波数方向の平滑化): pooled[ch] = Σ ch-1..ch+1 の発火
// leaky 積分: integ[ch] += pooled; integ[ch] -= STELLATE_LEAK
// chopper 規則発火: integ が閾値超えで発火 + リセット (rate ∝ 強度)
STELLATE_LEAK = 弱め      // 持続的な rate を維持
```

**生物対応**: multipolar = 周波数横断統合 (プール)、 chopper = 規則発火 (積分+リセット)。

**出力**: 20 ch (スペクトル包絡、 現状成分とほぼ同じ)。

### 3.4 出力形式と M1 接続

合計出力チャネル: 4 (Octopus) + 20 (Bushy) + 20 (Stellate) = **44 ch**

M1 接続の選択肢:
- **案 X**: M1 入力を 20 → 44 に拡張 (io_layout 一般化で対応可)
- **案 Y**: 3 ストリームを別々の M1 入力群へ (生物の並列経路に忠実)
- **案 Z (最小)**: まず Octopus 4ch を既存 20ch に追加 (24ch) して効果確認

---

## 第 4 部: 実装ロードマップ

### Step 1: Octopus 最小実装 (最速検証)
- `cochlear_nucleus.rs` 新規、 Octopus 4 細胞のみ
- 蝸牛 20ch → Octopus 4ch を生成、 M1 入力に追加 (案 Z、 24ch)
- M1 を 24 入力で構築 (io_layout or 簡易拡張)
- 5K 訓練 → per-pair between が 1.0 から下がるか検証
- **判定**: オンセットだけで M1 がアトラクタを脱出するか

### Step 2: Bushy 追加
- Octopus が効けば Bushy 20ch を追加 (44ch or 24ch)
- 周波数別 transient で更に分化するか

### Step 3: Stellate 統合 + 3 ストリーム最終形
- 3 細胞型全て、 M1 入力を最終構成に
- 5K/10K で selectivity / between を baseline (0.389/0.600) と比較

### Step 4: mo 無応答バグ修正 (並行)
- mo 音素が cochlea 全帯域 0 の原因究明 (音素合成 or cochlea パラメータ)

---

## 第 5 部: 設計判断の確認事項

1. **Octopus の coincidence 閾値**: [3,5,8,12] は妥当か? (20 帯域中 何個同時で「オンセット」 とするか)
2. **Bushy の transient 検出方式**: 立ち上がりエッジ検出 vs 微分ベース、 どちらか
3. **Stellate のプール幅**: ±1 帯域 (3 ch プール) で適切か
4. **M1 接続**: 案 Z (最小 24ch) で始めて段階拡張、 で良いか
5. **各細胞を ThermoNeuron で実装するか、 専用軽量セルにするか**
   - ThermoNeuron: 哲学的に統一的だが パラメータ調整が複雑
   - 専用セル: 直接的で制御しやすい (推奨、 cochlea の FireGenerator と同じ路線)

---

## 第 6 部: 期待される効果と検証

| 指標 | 現状 (M0→M1 直結) | 期待 (M0.5 経由) |
|---|---|---|
| per-pair M1 出力 between | 0.956〜1.000 | distinct 音素で < 0.7 |
| POST selectivity | 0.389 | > 0.5 (目標 0.6+) |
| active outputs | 12/40 | 増加 (アトラクタ分散) |
| mo 応答 | 0 (無応答) | Step 4 で修正後に応答 |

仮説が正しければ、 Octopus 単独 (Step 1) でも per-pair between の低下が観測されるはず。 観測されなければ仮説の再検討 (M1 のアトラクタが入力時間構造だけでは破れない = より深い問題)。
