# note 公開用 記事 2 本目

下記をそのまま note にコピー&貼り付け推奨。

---

## 推奨設定

### タイトル

```
Phase 2: 熱力学的描像で「学習」を物理プロセスに書き換える
```

代替案:
- 「『学習』とは何か — 熱力学から再構築する」
- 「Prigogine 散逸構造論で動く AI」

### 想定読者
- 技術中級 (1 本目を読んで興味を持った人)
- 「学習」のメカニズムを物理的に理解したい人
- 神経科学・物理学・計算機科学の交差点に興味ある人

### 読了時間
10-15 分

### タグ
```
AI
SNN
神経科学
熱力学
物理
ニューラルネットワーク
研究
Rust
オープンソース
Prigogine
```

### 価格
**無料** (Sponsors 向け早期アクセスは 1 週間限定で運用可)

---

## 記事本文 (コピペ用)

# Phase 2: 熱力学的描像で「学習」を物理プロセスに書き換える

[前回の記事](https://note.com/.../n/xxx) で、私の個人研究プロジェクト「**drpnn**」の概要を紹介した。

drpnn は、ChatGPT とはまったく異なる方向の AI を作る研究プロジェクトだ。整数演算のみ、確率なし、決定論的物理プロセスで「学習」を実現することを目指している。

今回はその核心 — **「学習」を熱力学プロセスとして書き換える** — について詳しく書く。

## 「学習」とは普通何か

普通の深層学習 (PyTorch、TensorFlow など) で「学習」と呼ばれるものは、こんなプロセスだ:

```python
for epoch in range(num_epochs):
    for batch in dataloader:
        outputs = model(batch)            # 推論
        loss = criterion(outputs, labels)  # 損失計算
        loss.backward()                   # 勾配計算
        optimizer.step()                  # 重み更新
```

- **損失関数** で「正解からのズレ」を測る
- **逆伝播** でズレを各重みに分配
- **勾配降下** で重みを調整

これらはすべて **数学的な操作** であって、物理的な実体はない。

しかし — 生物の脳には **損失関数も逆伝播も存在しない**。それでも学習する。なぜか?

## 脳における学習 = 物理プロセス

生物の脳で起きている「学習」を細胞レベルで分解すると、こうなる:

- ニューロン A が発火し、すぐ後にニューロン B が発火した
- A→B のシナプスで、ある分子 (CaMKII など) のリン酸化が起きる
- 結果として A→B の信号伝達効率が上がる (LTP: 長期増強)

これは **化学反応の積み重ね** であって、「損失を最小化しよう」という意図はどこにもない。

つまり脳における「学習」は、**意図的な操作ではなく、物理化学プロセスの結果として起きる**。

## drpnn の核心アイデア — 熱力学

drpnn は、これを **非平衡熱力学** の枠組みで実装する。

具体的には、各ニューロンを次のように設計する:

```rust
struct ThermoNeuron {
    membrane: i32,              // 膜電位 (整数)
    available_enthalpy: i32,     // 発火可能エネルギー (整数)
    local_entropy: i32,          // 蓄積された「熱」(整数)
    threshold_base: i32,         // 基準閾値
    // ... 他にも色々
}
```

そして各クロック (シミュレーション 1 step) で、こんな物理プロセスを実行する:

1. **入力流入**: 隣のニューロンから来た信号を、膜電位に加算
2. **熱の自然散逸**: `local_entropy` を時間とともに少しずつ減らす (放熱)
3. **発火条件**: `membrane >= threshold_base + local_entropy` かつ `enthalpy > 0`
4. **発火時の物理**:
   - `enthalpy -= 3` (エネルギー消費)
   - `local_entropy += 10` (熱生成)
   - `membrane = 0`

ここで重要なのは — **これは「判定」ではなく「物理」** だということだ。

「閾値を超えたら発火」と書くと判定に聞こえるが、これは「コップに水が一杯まで溜まったら溢れる」と同じこと。**条件式は判定ではなく、物理現象の記述**。

## シナプスの熱力学

シナプスも同じ枠組みで設計される:

```rust
struct ThermoSynapse {
    pre: usize, post: usize,
    delay: i32,
    conductance: i32,    // 熱伝導度 (信号伝達効率)
    vitality: i32,       // 結線の「生きてる度」
    alive: bool,
}
```

- **conductance**: シナプス強度。STDP (発火タイミング依存可塑性) で増減
- **vitality**: 信号通過で +1、時間で -1。0 になると `alive = false` (刈り取り)

「学習」と呼ばれる現象は、これらの整数値が動的に変化する **物理プロセスの集積** として立ち上がる。

## 軸索成長 — 熱勾配に沿った経路形成

drpnn の特徴的な機構の一つに **軸索成長** がある:

- 局所エントロピー (`local_entropy`) が高い (= 「熱い」) ニューロンは
- 隣接する PE のうち、最も「冷たい」(エントロピー低い) 相手に新しいシナプスを伸ばす

これは生物の脳における **軸索ガイダンス** (神経成長因子による経路選択) の単純化版だ。

```rust
fn axon_growth_step(neurons, synapses, topology) {
    for n in neurons.iter() {
        if n.local_entropy < GROWTH_THRESHOLD { continue; }

        let coldest = n.neighbors()
            .iter()
            .min_by_key(|nb| nb.local_entropy)
            .unwrap();

        if coldest.local_entropy < n.local_entropy - DELTA {
            create_synapse(n, coldest);
            n.local_entropy -= GROWTH_COST;  // 成長にもコスト
        }
    }
}
```

これにより、ネットワークの結線パターンは **熱勾配に沿って自律的に変化する**。意図的に「ここを繋ごう」と決めるのではなく、熱の流れが新しい配線を作る。

## Prigogine の散逸構造論

これら全体を統合すると、見覚えのある枠組みに行き着く。

**Ilya Prigogine** (1977 年ノーベル化学賞) の **散逸構造論** だ。

Prigogine が示したのは:

> 「外部から **エネルギーが流入** する **非平衡系** では、**散逸 (= エントロピー生成)** を効率化する **構造** が自発的に形成される」

例:
- ベナール対流 (温度差で流体に渦が自発形成)
- ベロウソフ・ジャボチンスキー反応 (化学振動)
- 生命そのもの (代謝による低エントロピー状態の維持)

drpnn では、外部刺激 = エネルギー流入、ネットワーク = 散逸系、形成される結線パターン = 散逸構造、と読み替える:

```
外部スパイク刺激 (エネルギー流入)
   ↓
ニューロン発火 (エンタルピー消費 + エントロピー生成)
   ↓
熱勾配に沿った軸索成長
   ↓
特定経路への信号集中 (LTP による conductance 強化)
   ↓
散逸を効率化する経路パターン形成 = 「学習」
```

つまり、**「学習」とは散逸構造の動的平衡状態への到達** として定義される。

これは哲学的なアナロジーではない。drpnn のソースコード上で、文字通りこのプロセスが整数値の変化として実行される。

## 「学習」関数は存在しない

drpnn のコードには、`apply_learning()` や `train()` のような明示的な学習関数は **一切存在しない**。

メインループはこれだけだ:

```rust
loop {
    for each clock {
        1. 入力配送
        2. 各ニューロン更新 (物理プロセス)
        3. 発火したニューロンのシナプス更新 (STDP)
        4. シナプス経由でスパイク配送 (遅延付き)
        5. 軸索成長を周期的に試行
    }
}
```

これだけ。「学習フェーズ」も「評価フェーズ」も区別しない。**ただ時間が経過するだけ**。

そして、外部刺激が時間構造を持っていれば、ネットワークは自然に散逸構造を形成する。それを我々が後付けで「学習した」と呼んでいる。

これは、生物の脳が「ただ生きているだけで学習する」という事実と、構造的に同じだ。

## 結果として何が起きるか

具体的な観察結果として、こんなことが起きる:

1. **同じ入力刺激には、同じ出力パターンが返る** (再現性 95%)
   - これは「決定論的物理プロセス」だから当然
2. **異なる刺激には、異なる出力パターンが返る** (類似度 16%)
   - 散逸構造が刺激ごとに異なる経路で形成されるから
3. **訓練後、外部刺激なしでも、ネットワーク内部で「学習した刺激パターンの状態」が自発的に再生される** (Welch t = 4.169, p < 0.001)
   - 池谷裕二教授 (東大薬学) が 2005 年に「自発活動は単なるノイズではない」と主張した現象を、Phase 2 で定量的に再現

3 番目は特に重要だ。**「学習」が、外部刺激への応答能力だけでなく、内部状態のレパートリ形成として実現している** ことを示している。

これは Kenet 2003 ([Nature 425:954](https://www.nature.com/articles/nature02078)) が視覚野で観察した現象 — 「皮質には内部状態のレパートリがあらかじめ存在し、外部刺激はそれを微調節するか、規定の内部レパートリから特定の状態を選択するだけ」(池谷 2005) — の **整数決定論版での再現** だ。

確率や浮動小数を使わずに、皮質の本質的な振る舞いを再現できる。これは drpnn 設計哲学の決定的な実証だと考えている。

## なぜこの方向が重要か

LLM の限界は明らかだ:

- **エネルギー消費** が持続不可能なレベル
- **学習データ** が枯渇に向かう (高品質テキストはほぼ全部使われた)
- **エッジで動かない** ため、応用範囲が限定される

drpnn の方向はこれと真逆だ:

- ニューロン 440 個 (約 700 KB メモリ) で動作
- 整数演算のみ (浮動小数演算ユニット不要)
- DRP のような特殊チップで、消費電力ミリワット級を目指す
- 学習データではなく **環境の時間構造そのもの** から学ぶ

これが完成すれば、補聴器、スマートウォッチ、ペット型ロボット、家電のリモコン — そういった日常デバイスに「本物の知能」を載せられる可能性がある。

ChatGPT を小さくするのではなく、**脳から学んで一から作り直す**。

## 次回予告と支援のお願い

次回は、軸索成長の実装と、訓練中に形成される散逸構造の可視化について書く予定。GIF や動画も用意したい。

drpnn は個人プロジェクトで、CC BY-SA 4.0 で公開している。研究を継続するために、GitHub Sponsors で支援を募集している。

- **GitHub (コードと論文)**: https://github.com/vivacchi/drpnn
- **GitHub Sponsors (継続支援)**: https://github.com/sponsors/vivacchi

月 1 ドル (約 150 円) から支援できる。「研究そのものを応援する」形だ。

ここまで読んでくれてありがとう。技術的な質問・コメントは note のコメント欄か GitHub Issues でどうぞ。

---

*この記事は CC BY-SA 4.0 で公開されています。引用・転載・改変自由 (帰属表示 + 同条件継承)。*

*次回予告: 「軸索成長を可視化する — 散逸構造形成のリアルタイム観察」(可視化編)*
