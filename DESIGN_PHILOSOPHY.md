# 設計哲学 (DESIGN PHILOSOPHY)

最終更新: 2026-05-22
ステータス: プロジェクト哲学的中核、確定版

このドキュメントは、本プロジェクトの根本的な設計思想を明文化したものです。**全ての実装判断、機構の追加・削除、評価方法の選択は、この哲学に照らして判断する**こと。

---

## 1. プロジェクトの究極目標

**究極の省エネ・省リソース・高機能 AI を、生物の脳を模倣することで実現する。**

具体的には、Renesas DRP (Dynamically Reconfigurable Processor) などの動的再構成ハードウェア上で動作する、自律成長型 1 ビット Spiking Neural Network。

---

## 2. 6 つの設計原理

これまでの議論で確立された、絶対に守るべき原理。

### 原理 1: 局所性 (Locality)

各ニューロンとシナプスは、**物理的に接続された相手とのみ情報を交換する**。

- ニューロンは自分に入ってくる電気信号、自分の内部状態のみを知る
- シナプスは接続している前後のニューロンの状態のみを知る
- 「ネットワーク全体の発火率」「平均活動レベル」のようなグローバル状態は、どの要素も知り得ない
- グローバル変数を観測する仕組みは設計に含めない

### 原理 2: 物理性 (Physicality)

各要素の振る舞いは、**生物の細胞内で起きている物理プロセスの単純化版**として記述される。

- 判断機構、評価関数、目的関数は一切含まれない
- 「target_rate との比較」「閾値超えたら判定」のような判断は禁止
- あるのは「物理量の積算」「閾値超過による物理現象 (発火など)」「時間経過による緩和」のみ
- これらの組み合わせから振る舞いが**創発的に**立ち上がる

### 原理 3: 決定論性 (Determinism)

**確率や乱数を使わない**。同じ初期状態と同じ入力からは、必ず同じ結果が出る。

- 確率的伝送失敗、ランダムプルーニングなどは設計違反
- ただし複雑系としてのカオス的振る舞い (初期値鋭敏性、リミットサイクル) は許容
- これらは確率ではなく、決定論的な系の自然な性質
- 完全な再現性によりデバッグと検証が容易になる

### 原理 4: 整数演算 (Integer Arithmetic)

**すべての状態は整数 (または有限ビットの量) で表現する**。

- 連続値の浮動小数点演算は使わない (近似でも避ける)
- 1 ビットスパイク、N ビット整数の膜電位など
- これは DRP 実装との整合性とエネルギー効率のため
- 生物の脳も実質的に離散的 (スパイクは 1 ビット、化学物質濃度も離散的に観察可能)

### 原理 5: 創発的振る舞い (Emergent Behavior)

ネットワーク全体の機能 (学習、認識、フィンガープリント生成) は、**局所機構の相互作用から創発的に立ち上がる**。

- トップダウンに「こうあるべき」と設計しない
- 局所機構が物理的に正しければ、全体の振る舞いは自然に生まれる
- これは生物の脳が実際にやっていることと同じ
- 設計者の役割は局所機構を正しく作ること、振る舞いを定義することではない

### 原理 6: 平衡としての学習 (Learning as Equilibrium)

「学習」は明示的なプロセスではなく、**系が環境と相互作用しながら到達する動的平衡状態**。

- 学習させるための特別な機構 (apply_learning など) は存在しない
- 物理プロセスが続いた結果として、系が安定状態に落ち着く
- その安定状態が「学習済み」と呼ばれる
- 損失関数、目的関数、評価関数なしに学習が起きる

---

## 3. 学習の二分類

ユーザーが提示した極めて重要な区分。これに従ってモジュール責務を分離する。

### タイプ 1: 自己学習 (Self-Learning / Evolution)

**定義**: 系と環境の物理的相互作用の結果として、系が動的平衡状態に到達するプロセス。

**特徴**:
- 教師、目的関数、評価関数は不在
- 機構はすべて局所的な物理プロセス
- 結果は「環境との整合性」として現れる
- 進化・進歩と呼べる性質

**例**: 言葉を話す人々の中で育つと、自然に話せるようになる (環境との物理的相互作用の結果)

**担当モジュール**: M0 (蝸牛)、M1 (一次聴覚野)、M2 (二次聴覚野)、M3 (カテゴリ野) など、感覚処理階層

### タイプ 2: 教示学習 (Norm Acquisition)

**定義**: 人類が築き上げてきた文明的標準 (言語、文字、数学記号、文化的規範) を獲得するプロセス。

**特徴**:
- 教師による外部からの対応付けが必須
- ただし機構自体はタイプ 1 と同じ (ヘブ則、STDP)
- 違うのは「環境からの自然入力」に加えて「教師からの強い同期信号」が入る点
- 個体・地域・時代によって獲得内容が異なる (文明依存)

**例**: 言葉と文字を関連付ける (「あ」という音と「あ」という文字の対応は人類社会の取り決め)

**担当モジュール**: M5 以降 (連合野、報酬系、言語処理階層)

### 両者の関係

**機構は同じ、入力構造が違うだけ**:
- どちらもヘブ則・STDP・物理プロセスで動く
- タイプ 1: 環境入力のみ
- タイプ 2: 環境入力 + 教師からの同期信号

**階層的関係**:
- タイプ 1 は基盤、タイプ 2 はその上に乗る
- タイプ 1 なしにタイプ 2 はあり得ない (音が認識できないと、音と文字の対応も学べない)
- 一方、タイプ 1 は単独で機能する

**実装順序**:
- M0, M1 (タイプ 1) を先に完成させる
- その上に M5 (タイプ 2 が混じる) を載せる
- さらに高次モジュール (タイプ 2 が本格化) を載せる

### 過去の失敗の診断

これまでの 13 試行の失敗は、**タイプ 1 のモジュール (M1) にタイプ 2 の機能 (target 指定、外部報酬による正解判定) を入れた階層違反**だった。

A1 は「これは『あ』」と知らない。「あ」というラベルは Wernicke 野が後付けする。A1 はただ音の時空間パターンに対応するフィンガープリントを出すだけ。

target_a/target_b を指定して報酬学習させていたのは、M1 に M5 の機能を押し込もうとしていたこと。これが Neuron 23 の逆転、selectivity の不安定、PERSIST 崩壊などすべての症状の根本原因。

---

## 4. M1 の責務 (最終確定版)

### M1 が行うこと

**入力スパイクパターン空間から出力フィンガープリント空間への、連続的で再現性のある写像 F を実装する。**

任意の入力 X に対して、F(X) は以下を満たす:

1. **決定論性 (Determinism)**: 同じ X に対する F(X) は安定 (ノイズに対して再現性がある)
2. **連続性 (Continuity)**: X に近い X' に対する F(X') は F(X) に近い (滑らかな写像)
3. **識別性 (Distinctness)**: X と本質的に異なる Y に対しては F(X) ≠ F(Y)
4. **安定性 (Stability)**: 経験を積んでも F(X) は大きく変わらない (継続学習可能性)
5. **容量 (Capacity)**: 区別可能な入力パターンの数が十分大きい

### M1 が行わないこと

- 「意味」の付与 (これは M5 以降の責務)
- 「カテゴリ」の形成 (これは M2-M3 の責務)
- 「重要な音と無視すべき音」の選別 (これは注意系 M9-M10 の責務)
- 「正解」の判定 (タイプ 2 学習に属し、M5 以降の責務)
- 不変性 (同じ「あ」の異なる発音を同一視) の獲得 (これは M2 の責務)

### 連続性の重要性 (補足)

M1 の核心的価値は「連続性」にある。

入力に微小な摂動が入ると (マイク劣化、環境ノイズ、個体差):
- 連続性がない場合: フィンガープリントが大きく変わる → 上位階層が「別の音」と判断 → システム全体が脆い
- 連続性がある場合: フィンガープリントも微小にだけ変わる → 上位階層が「補正可能な近所」と判断 → システム全体が頑健

連続性は M1 が上位階層に提供する基盤サービス。**不変性は M2 以降が担うが、それが可能なのは M1 が連続性を提供しているから。**

---

## 5. ハードウェアマッピング (DRP 実装)

このプロジェクトの最終形態は、Renesas DRP 上での動作。設計はその実装と完全に整合している。

### 対応関係

| 生物概念 | ソフトウェア表現 | DRP ハードウェア表現 |
|---|---|---|
| ニューロン | Neuron 構造体 | PE (Processing Element)、またはスイッチ要素 |
| シナプス | Synapse 構造体 | 配線 (相互結合網の配線、crossbar 交点) |
| スパイク | 1 ビット信号 | 1 ビットの配線上の信号 |
| 軸索遅延 | delay フィールド (整数クロック数) | 配線経路長、または遅延素子 |
| 結線の有無 | exists フラグ | スイッチの ON/OFF |
| 結線強度 | connection_strength (整数) | スイッチ ON/OFF の閾値判定の元 |
| 構造的可塑性 (学習) | exists の動的変更 | DRP の動的再構成 |

### DRP 特性との完全一致

DRP の特性:
- 多数の PE が並列に動作
- PE 間接続が動的に再構成可能
- 整数演算に最適化
- ローカルなデータフローが効率的

我々の SNN 設計:
- ニューロンが並列に動く (= PE が並列に動く)
- シナプス open/close を動的に変える (= 配線を動的に再構成する)
- すべて整数演算
- 各要素はローカル情報のみ使用 (原理 1: 局所性)

**「学習」と「DRP の再構成」が物理的に同一視できる**。これがこのプロジェクトの核心アイデア。通常の DNN を DRP に乗せるのではなく、DRP の動的再構成機能そのものを学習機構として活用する。

### 1 ビット信号の重要性

なぜ 1 ビットなのか:

1. **配線リソース極小化**: 多ビット並列ではなく 1 ビット、配線幅が最小
2. **時間多重化が自然**: スパイクの「時刻」が情報を担う (空間ではなく時間でエンコード)
3. **エネルギー効率**: 信号遷移が最小、消費電力が低い
4. **生物的妥当性**: 実際の神経細胞も 1 ビット (発火するかしないか)
5. **DRP との整合**: スイッチの ON/OFF と直結

省エネ・省リソースという究極目標との完全な整合性。

---

## 6. 物理的に正しい機構の実装方針

原理 2 (物理性) に従い、これまで判断機構として実装されていた箇所を物理プロセスに書き直す。

### ニューロンの状態 (すべて整数)

```rust
struct Neuron {
    membrane: i32,             // 膜電位 (積分された入力)
    threshold: i32,            // 発火閾値 (動的、整数)
    refractory_remaining: i32, // 不応期残り
    readiness: i32,            // 発火準備度 (神経伝達物質の在庫に対応)
    last_spike_time: i32,      // 最終発火クロック
}
```

各クロックでの更新ルール (判断機構なし、物理プロセスのみ):

```
1. 不応期処理: refractory > 0 なら -1 して終了
2. 膜電位積分: membrane += 入力電流, リーク分を引く
3. 閾値の自然回復: threshold が baseline より高ければ徐々に下がる
4. 準備度の自然回復: readiness が max より低ければ徐々に上がる
5. 発火判定: membrane >= threshold かつ readiness > 0 なら発火
   - membrane = 0, refractory セット
   - readiness -= 1 (伝達物質消費)
   - threshold += 増分 (短期的に疲労)
```

「target_rate と比較して調整」のような判断は存在しない。発火するたびに何かが消耗し、時間で回復する、という物理プロセスのみ。

### シナプスの状態 (すべて整数)

```rust
struct Synapse {
    pre: usize,
    post: usize,
    delay: i32,                   // 軸索遅延 (クロック数)
    connection_strength: i32,     // 物理的接続強度 (整数)
    exists: bool,                 // strength が閾値以上か (自動更新)
}
```

STDP の物理的実装:
```
post 発火時、近傍に pre 発火があれば: connection_strength += LTP_AMOUNT (因果強化)
pre 発火時、近傍に post 発火があれば: connection_strength -= LTD_AMOUNT (反因果弱化)
時間経過: connection_strength が使われないと徐々に減衰
exists の更新: connection_strength >= OPEN_THRESHOLD なら true、それ以外 false
```

cumulative_sign のような累積判定は不要。STDP が直接 connection_strength を変える物理プロセス。

### 明示的な学習関数の廃止

apply_self_organization、apply_reward、apply_homeostasis などの「学習を起こす関数」は存在しない。

メインループは:
```
loop {
    for each clock {
        1. 入力配送 (外部刺激を該当ニューロンに)
        2. 各ニューロン更新 (上記の物理プロセス)
        3. 発火したニューロンのシナプス更新 (STDP)
        4. シナプス経由でスパイク配送 (遅延付き)
    }
}
```

明示的な「学習フェーズ」も「評価フェーズ」もない。ただ時間が経過するだけ。学習は時間の関数として自然に進行する。

---

## 7. 評価方法

M1 の評価はタイプ 1 (自己学習) の視点のみで行う。

### 評価する性質

5 つの性質を測定:

1. **決定論性**: 同パターン内のフィンガープリント類似度 > 0.85
2. **連続性 (最重要)**: 入力摂動に対する出力変化が滑らか
3. **識別性**: 異なるパターン間の類似度が低い (within > between)
4. **安定性**: 追加経験後も F(X) が大きく変わらない
5. **容量**: 区別可能なパターン数

### 評価しないこと

- 「正解への一致率」(タイプ 2 評価、M1 の責務外)
- 「分類精度」(同上)
- 「特定パターンの認識率」(M1 はパターン分類器ではない)

### 連続性テストの詳細

特に重要な連続性は以下のテストで測定:

- **ジッタ耐性**: 入力スパイクのタイミングに ±1, 3, 5, 10ms のジッタ
- **欠落耐性**: 入力スパイクの 10%, 30%, 50% をランダムに削除
- **追加耐性**: ランダムな追加スパイクを 10%, 30% 加える
- **置換耐性**: 入力ニューロンの 1-2 個を別のニューロンに置換

各摂動量に対して F(X) と F(X+ε) の類似度を測り、**摂動量に比例して滑らかに低下する曲線**になっていることを確認。「変化なし」(完全な不変性) ではなく「連続的な変化」が望ましい。

---

## 8. 確率の排除と物理プロセスの優位性

ユーザーが指摘した「確率や不確定要素ではなく、単純物理現象の集合体で実装する」の意義。

### 確率を使うことの問題

- 「サイコロを振る」という非物理的な操作を含む
- 物理的根拠が薄い (実際の細胞内に乱数発生器はない)
- 結果の再現性が下がる
- ハードウェア実装で乱数生成器が必要、コスト増
- デバッグが困難

### 決定論的物理の利点

- 完全な再現性
- 物理的に検証可能な振る舞い
- DRP 実装の単純化 (整数演算と比較のみ)
- デバッグ容易
- 「なぜこの結果になったか」が物理的に説明可能 (完全な物理透明性)

### カオスと多様性

決定論的でも、複雑系では多様な振る舞いが現れる:
- 初期値鋭敏性
- リミットサイクル
- ストレンジアトラクタ

これらは確率ではなく、決定論的な系の自然な性質。我々の SNN もこういう振る舞いを示す。生物の脳もカオス的システム。確率を使わずに多様性を確保できる。

---

## 9. やってはいけないこと (再確認)

過去の失敗から学んだ、明確に避けるべき設計。

### 階層違反

- M1 にタイプ 2 機能 (target 指定、外部報酬、正解判定) を入れない
- 各モジュールの責務範囲を超えた機能を押し込まない
- 「全体最適化」のためにモジュール境界を破らない

### 判断機構

- target_rate との比較
- cumulative_sign のような閾値判定
- グローバルな評価関数
- 確率的伝送失敗
- 「○○なら××する」という条件分岐ベースの学習

### 連続値・浮動小数

- 浮動小数点演算
- 微小な確率値の操作
- 連続的なパラメータ調整 (整数化できるならする)

### LLM 的発想

- 「正解データセット」での訓練
- 損失関数の定義と最小化
- バックプロパゲーション
- Attention 機構
- 大規模パラメータの統計学習

これらは本プロジェクトの哲学と相容れない。意図的に避ける。

---

## 10. このドキュメントの位置づけ

このドキュメントは**プロジェクトの哲学的中核**。

今後の全ての判断は、ここに書かれた原理に照らして行う:

- 新しい機構を追加するか? → 原理 1-6 に違反しないか
- パラメータを調整するか? → 原理 2 (物理性) を保っているか
- 評価指標を変えるか? → タイプ 1 評価の枠内か
- モジュール間の連携を変えるか? → 学習の二分類とモジュール責務を尊重しているか

迷ったらこのドキュメントに戻る。

このドキュメントの内容を変更する場合は、**ユーザーとの明示的な合意**が必要。Claude (AI 側) の判断だけで原理を曲げない。

---

## 11. 非平衡熱力学系としての SNN (Phase 2 設計の基盤)

2026 年 5 月の議論で、本プロジェクトの全体を**非平衡熱力学系**として記述するという、より深い設計基盤が確立された。これは Phase 1 (物理置換型) を否定するものではなく、その背後にある統一原理を明らかにするものである。

### 11.1 統一原理

これまでの各機構を熱力学的に再解釈すると、以下の対応関係が成立する:

| Phase 1 (物理置換型) の概念 | Phase 2 (熱力学型) の概念 |
|---|---|
| ニューロンの発火 | エンタルピー消費とエントロピー (熱) の生成 |
| readiness (神経伝達物質在庫) | 利用可能エンタルピー (available_enthalpy) |
| 動的閾値 (threshold) | 局所エントロピー蓄積による物理的閾値上昇 |
| connection_strength | シナプス経路の熱伝導度 (信号 = エネルギー伝達効率) |
| 慣化 (fatigue) | 局所エントロピー蓄積 |
| 慣化回復 | エントロピーの空間拡散と散逸 |
| 構造的可塑性 (シナプス open/close) | 熱勾配に沿った軸索の自律的成長 |
| 学習 | 系全体のエントロピー生成を最小化する散逸構造の形成 |
| 動的平衡 | 散逸構造としての定常状態 |

これらは別々の機構ではなく、**単一の熱力学的描像から導かれる必然的な振る舞い**である。

### 11.2 各要素の熱力学的記述

#### ニューロンの状態

```rust
struct ThermodynamicNeuron {
    membrane: i32,              // 膜電位
    available_enthalpy: i32,    // 利用可能エンタルピー (発火可能エネルギー)
    local_entropy: i32,         // 局所エントロピー (蓄積された熱)
    threshold_base: i32,        // 基準閾値
    refractory_remaining: i32,  // 不応期残り
    last_spike_time: i32,
}
```

各クロックでの物理プロセス:

1. **エンタルピー流入**: 入力スパイクが届くと膜電位が上昇 (シナプス経由のエネルギー伝達)
2. **エントロピー散逸**: local_entropy が時間とともに減衰 (熱が周囲に拡散)
3. **エンタルピー回復**: available_enthalpy が時間とともに上昇 (上限あり)
4. **発火条件**: membrane >= (threshold_base + local_entropy) かつ available_enthalpy > 0
   - 局所エントロピーが高い (= 熱い) ほど、実効閾値が上がる (発火しにくい)
5. **発火時**:
   - available_enthalpy -= 1 (エンタルピー消費)
   - local_entropy += K (発火による熱生成)
   - membrane = 0
   - refractory_remaining セット

これにより、慣化機構は明示的な機構ではなく、エンタルピー消費とエントロピー蓄積の自然な帰結となる。

#### シナプスの状態

```rust
struct ThermodynamicSynapse {
    pre: usize,
    post: usize,
    delay: i32,
    conductance: i32,    // 熱伝導度 (信号伝達効率)
    exists: bool,        // conductance >= OPEN_THRESHOLD で自動 true
}
```

STDP の熱力学的解釈:

- pre が先、post が後 (因果): 経路がエネルギーをうまく伝えた → conductance += LTP_AMOUNT
- post が先、pre が後 (反因果): 経路が役に立たなかった → conductance -= LTD_AMOUNT
- 時間経過: 使われない経路の conductance は緩やかに減衰 (放熱による劣化)

### 11.3 軸索成長 (熱勾配誘導)

熱力学的描像で最も特徴的な新機構が、**熱勾配に沿った軸索成長**である。

#### 基本ルール

各ニューロンは、自分の local_entropy が高い (熱い) 場合、隣接する PE のうち**最も冷たい (local_entropy が低い) PE** へ新しい結線を伸ばす。

```
N 試行に 1 回 (または特定の条件で):
  for each neuron n:
    if n.local_entropy > GROWTH_THRESHOLD:
      cold_neighbor = argmin(neighbor.local_entropy for neighbor in adjacent(n))
      if cold_neighbor.local_entropy < n.local_entropy - DELTA:
        create_synapse(n, cold_neighbor, initial_conductance = MIN_CONDUCTANCE)
        n.local_entropy -= GROWTH_COST  # 成長にもコストがかかる
        cold_neighbor.local_entropy += HEAT_TRANSFER  # 結線で熱が伝わる
```

#### 隣接性の定義

「物理的に隣接」を、ニューロン配置上で定義する。例えば:

- ニューロンを 2 次元グリッドに配置 (DRP の物理 PE 配置に対応)
- 各ニューロンの隣接は、上下左右 + 斜め (8 近傍) または上下左右 (4 近傍)
- これにより、軸索成長は局所的にしか広がらず、原理 1 (局所性) を保つ

#### 飽和回避メカニズム

提案文書 (議論ログ) で確認された 3 つの飽和回避メカニズムを実装する:

**メカニズム A (成長のコスト)**:
軸索成長そのものが GROWTH_COST 分のエントロピーを消費する (発熱する)。これにより、冷たい PE が急に「冷たくなくなる」ため、全員が一斉に同じ場所に群がる現象が物理的にブロックされる。

**メカニズム B (維持コスト = 焼き切れ)**:
形成された結線が、Polychronization に貢献せず無駄に発熱を続けると、conductance が時間で減衰し、最終的に閾値を下回って exists = false に戻る。共鳴した結線だけが長期的に維持される。

**メカニズム C (空間的距離)**:
軸索成長は隣接 PE のみに限定される。これにより、結晶成長のような秩序ある拡大が起こり、大規模な集団移動 (空間カオス) を防ぐ。

### 11.4 学習 = 散逸構造の形成

これらすべてを統合すると、本システムの「学習」は次のように定義される:

**外部環境からスパイクが流入 → エンタルピーが系に注入される → 一部の経路が共鳴して効率的にエネルギーを散逸できる構造を形成 → その構造が動的平衡 (散逸構造) として安定化する**

これは Ilya Prigogine の散逸構造論 (1977 ノーベル化学賞) の枠組みそのものである。生命を「エネルギー流のもとで自己組織化する非平衡系」として捉える視点が、計算機上で再現される。

### 11.5 Phase 1 と Phase 2 の並行実装

Phase 1 (物理置換型) は既に部分的に実装されている。Phase 2 (熱力学型) を 0 から構築し、両者を並行して評価することで、以下が明らかになる:

1. **両者の振る舞いの一致度**: 理論的には等価のはずだが、実装上の差異がどう現れるか
2. **熱力学的描像の優位性**: 軸索成長のような Phase 1 にはない機構が、性能に貢献するか
3. **設計の美しさ vs 実装の現実性**: 統一的描像と個別最適化のトレードオフ

これは単なる二重実装ではなく、本プロジェクトの理論的基盤を検証する科学的実験である。

### 11.6 概念実装の選択

熱力学的描像は、原理的にはシリコン上の実際の物理熱を学習機構として利用できる。しかし量産時の個体差を考えると、現段階では**概念実装** (整数値で熱力学量を表現) を採用する。

これにより:
- 完全な複製可能性が保証される (個体差ゼロ)
- デバッグと検証が容易
- DRP 実機への移植時も、同じ概念が成立する

将来的に、実物理熱を学習機構として使うバリアント (例えば、PE の温度センサーからのフィードバックを軸索成長に反映する) を試みる余地は残す。これは Phase 3 以降の検討事項である。

