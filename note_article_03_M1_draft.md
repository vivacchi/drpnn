# note 公開用 記事 3 本目 (M1 詳細解説)

下記をそのまま note にコピー&貼り付け推奨。

---

## 推奨設定

### タイトル

```
M1 一次聴覚野を物理から作る — 構造的可塑性と動的平衡
```

代替案:
- 「『脳の一部』を 440 個のニューロンで実装する」
- 「皮質を確率なしで作る — drpnn の M1 詳細」
- 「Fork F-G1-R1 — 13 回の失敗から辿り着いた M1」

### 想定読者
- 技術中級〜深い (1, 2 本目を読んだ人)
- ニューラルネットワークの内部構造に興味ある人
- 神経科学者・組み込み開発者の交差点

### 読了時間
12-18 分

### タグ
```
AI
SNN
神経科学
シナプス可塑性
脳科学
Rust
ニューラルネットワーク
STDP
オープンソース
研究
```

### 価格
**無料** (Sponsors 早期 1 週間限定可)

---

## 記事本文 (コピペ用)

# M1 一次聴覚野を物理から作る — 構造的可塑性と動的平衡

[前々回](https://note.com/.../n/xxx) で drpnn の概要を、[前回](https://note.com/.../n/xxx) で熱力学的な「学習」の枠組みを紹介した。

今回はその中核 — **M1 (一次聴覚野) の具体的な実装** — を詳しく書く。「設計」の話ではなく「実物」の話だ。

## M1 = A1 = 一次聴覚野

drpnn は 12 個のモジュールから構成される (M0 蝸牛、M1 一次聴覚野、M2 二次聴覚野、…、M11 小脳)。このうち現在実装が完了しているのは M0 と M1 の 2 つだ。

M1 は脳でいう **A1 (一次聴覚野)** に相当する。蝸牛 (M0) から来る音響スパイク列を受け取り、それを「時空間パターン → フィンガープリント」へと変換する装置。

決定的なのは、M1 が **「意味」を一切扱わない** こと。「これは『あ』という音だ」「これは音楽だ」というラベルは付けない。ただ、入力刺激を「特定の出力スパイクパターン」へと再現性ある形で変換するだけ。

意味付けは M5 (報酬系) 以降の責務で、M1 はそこまでの **「連続写像装置」** に徹する。

## なぜ意味を扱わないか — 過去の失敗

これは設計哲学から導かれた結論だが、その背景には 13 回の失敗実験がある。

過去、R-STDP (報酬調節型 STDP) を使って、M1 にラベル教師を持ち込もうとした。「これは A パターンだから報酬」「これは B パターンだから罰則」というやり方だ。

結果: **すべて失敗**。selectivity (識別性能) が 0.32-0.58 で頭打ちになり、訓練を進めると逆に崩壊する現象が頻発した。

診断: **「A1 にラベルを与えるのは階層違反」**。生物の A1 も、入力音にラベルを付けない。「これは『あ』」と認識するのは Wernicke 野 (言語野) の役割で、ラベルは後から付与されるものだ。

13 試行の失敗から、設計哲学が確立した:
- **タイプ 1 学習 (自己学習)**: 環境との物理的相互作用による動的平衡 → M0-M3 の責務
- **タイプ 2 学習 (教示学習)**: 人類の文明的標準を獲得 → M5 以降の責務

M1 はタイプ 1 のみを扱う。これだけが、ここでのルールだ。

## 構造 — 440 個のニューロン

現在の M1 (Fork F-G1-R1 v2) は、こんな構造をしている:

```
grid 20 × 22 = 440 セル、1 セル = 1 ニューロン

y=0          : 入力ニューロン 20 個 (M0 蝸牛からの聴神経)
y=1..19      : 内部 380 セル
               興奮性 304 個 + 抑制性 76 個 (シードベースのランダム配置)
y=20, y=21   : 出力ニューロン 40 個 (2 行 × 20 = 40)
```

各セルには 1 個のニューロンだけが居る。これは Renesas DRP の **1 PE = 1 ニューロン** 原則との整合性を保つため。

**抑制性ニューロンの比率は 18.1%**。これは大脳皮質の典型値 (15-20%、Markram et al. 2004) のど真ん中。

配置は seed=300 でシード固定の **ランダム** 配置。同じ seed なら同じ配置が再現される。決定論性 (原理 3) を保ちつつ、生物学的妥当性 (均一密度、Rockel et al. 1980) を再現する。

## 興奮性ニューロンと抑制性ニューロン — デールの原則

シナプスには「興奮性」「抑制性」の区別がない。代わりに、**送信元のニューロン** が興奮性か抑制性かで、信号の符号が決まる。

これは **デールの原則** (Dale's principle、1935) に従う:
> 「1 つのニューロンが放出する神経伝達物質は 1 種類だけ」

なので、`is_inhibitory` は **ニューロンの属性** であって、シナプスの属性ではない。送信時に符号を反転させる:

```rust
let signal = if pre_neuron.is_inhibitory {
    -conductance / SIGNAL_SCALE
} else {
    conductance / SIGNAL_SCALE
};
```

これにより、同じシナプス型を全ニューロンで使い回せる。実装の単純化と生物学的妥当性が両立する。

## シナプス可塑性 — STDP の整数実装

シナプスの強さ (`conductance`) は、**Spike-Timing Dependent Plasticity** (STDP) で動的に変化する。Bi & Poo (1998) の実験的観察を整数演算で再現:

```rust
// post 発火時に呼ばれる: 直近に pre が発火していたら因果 → 強化
fn update_on_post_spike_trace(&mut self, pre_spike_trace: i32) {
    if pre_spike_trace > 0 && pre_spike_trace < CAUSAL_WINDOW {
        self.conductance += LTP_AMOUNT;  // +5
    }
}

// pre 発火時に呼ばれる: 直近に post が発火していたら反因果 → 弱化
fn update_on_pre_spike_trace(&mut self, post_spike_trace: i32) {
    if post_spike_trace > 0 && post_spike_trace < CAUSAL_WINDOW {
        self.conductance -= LTD_AMOUNT;  // -6
    }
}
```

`CAUSAL_WINDOW = 160 step = 80ms` (生物の STDP 窓と整合)。
`LTD_AMOUNT (6) > LTP_AMOUNT (5)` で、シナプス強度の暴走を防ぐ (Song et al. 2000)。

## 構造的可塑性 — vitality と軸索成長

drpnn の特徴は、シナプス強度だけでなく **シナプスの生死** も動的に変わること。

各シナプスは `vitality` という値を持つ:

- 信号通過のたびに **+1** (使用された証)
- 時間経過で徐々に **-1** (使われない結線は劣化)
- `vitality > 0` が `alive`、`0` で死亡 (刈り取り)

これは生物の **use-dependent maintenance** (使用依存維持) に対応する。「使われない結線は消える」現象を物理プロセスとして実装。

そして、ニューロン側にも構造的可塑性がある: **軸索成長**。

```rust
// local_entropy が高い (= 熱い) ニューロンは
if neuron.local_entropy > GROWTH_THRESHOLD {
    // 隣接 8 セル (Moore 近傍) のうち、最も冷たい相手に
    let coldest = neighbors().min_by_key(|n| n.local_entropy).unwrap();
    if coldest.local_entropy < me.local_entropy - DELTA {
        // 新しいシナプスを伸ばす
        create_synapse(me, coldest);
        me.local_entropy -= GROWTH_COST;
    }
}
```

熱い (= よく発火している) ニューロンが、冷たい (= 受信余地のある) 隣接ニューロンへ自律的に結線を伸ばす。これは Prigogine 散逸構造論の「熱勾配に沿った構造形成」をニューロン版で実装したもの。

**8 近傍 (Moore 近傍)** にしているのは、4 近傍だと縦横にしか軸索が伸びず、斜め成長が物理的に不可能になるから。

## 過剰接続 + 発生学的刈り込み

初期化時、皮質内ニューロン同士は **過剰に結線** される (シナプス約 17,000 本)。これは:
- 入力→皮質: 1,600 本
- 抑制→興奮: 1,520 本
- 興奮→皮質: 13,760 本 (= 360 興奮性ニューロン × 40 fanout)

生物の発達期 (シナプス形成 → 刈り込み) を模倣している。実際に Fork F-G1-R1 v2 を 10,000 trial 訓練すると:

- 開始時: 約 17,000 本のシナプス、すべて alive
- 訓練 1,500 trial 付近から刈り込み開始
- 訓練 10,000 trial 終了時: alive 約 10,000 本、刈り取り 7,000 本

これは **「発生学的刈り込み」** の Phase 2 版だ。Mountcastle や Hubel & Wiesel が観察した皮質発達の現象を、整数決定論の物理プロセスで再現している。

## UP/DOWN 状態 — 多重アトラクタ

最近追加したのが **UP/DOWN 状態**。

生物の皮質ニューロンは、静止膜電位が 2 つの安定状態を持つ:
- **UP 状態**: 約 10 mV 脱分極、発火可能
- **DOWN 状態**: 通常の静止膜電位、発火しにくい

これは自発的に振動する (周期 100-300ms)。

drpnn では、これを各ニューロンに付与:

```rust
if self.up_state {
    self.membrane += self.up_offset;  // UP 時は膜電位ブースト
}
// 自発遷移 (個体差付き周期)
self.up_down_counter += 1;
if self.up_down_counter >= cur_period {
    self.up_state = !self.up_state;
    self.up_down_counter = 0;
}
```

確率は使わない。個体差は `idx % 31` のような決定論的個体差で実装。

これにより、ネットワークは「動的多重アトラクタ」を持つ。静音時にも内部活動が UP/DOWN リズムで構造化される。

## 時間 bin 化評価 — 聴覚に妥当な指標

学習の評価には、最初 **時間平滑化 fingerprint** を使っていた:

```
fingerprint_i = Σ exp(-(t_end - t_spike) / tau)
```

これは「最終時点での出力ニューロン活性度」を測る指標で、時間情報を τ=50ms で平滑化してしまう。聴覚的にはこれが問題で、「いつ発火したか」を捨ててしまう。

そこで **時間 bin 化 fingerprint** に置き換えた:

```
n_bins = 30 (10ms 刻み)
fp[i][b] = 出力 i が時間 bin b で発火した回数
flatten して 40 × 30 = 1,200 次元のベクトル
```

これで「いつ、どこで発火したか」が直接保持される。聴覚モデルとして本質的な情報。

評価指標を変えただけで、過去の M1 (Fork F-G1-R1 v1) の selectivity が 0.294 → **0.756** と劇的に上昇した。これは「データが変わった」のではなく「**正しく見るようになった**」という話。

過去の数値が低かったのは、評価指標が聴覚的に不適切だっただけだ。

## 「仮想 M0」等価性 — 確率共振の実装

更に最近の発見として、**M1 入力ニューロンの自発発火** を有効化すると selectivity が更に上がる。

```rust
// 入力ニューロンに、内部ニューロンと同じく自発入力 +2 を入れる
spontaneous_input: 2,
leak: 1,
```

これだけで、固定パターン 10k 訓練後 POST selectivity が **0.756 → 0.795** に上昇 (現時点最高値)。

これは何か? — **確率共振** (Anderson 2000) の Phase 2 版だ。

自発発火 (background noise) が、弱い刺激信号の検出能力を高める現象。Anderson が視覚野で観察し、Wiesenfeld & Moss (1995) が一般理論として確立した。

drpnn では、M1 入力ニューロンに自発電流を入れることで、これを物理プロセスとして実装している。確率モデルでなく、決定論的な電流加算で確率共振が再現される。

## 内部状態レパートリ — Kenet 2003 の再現

最新の発見が **内部状態レパートリ** の定量実証 (2026 年 5 月)。

Kenet et al. (2003, Nature 425:954) は、視覚野の **自発活動** が刺激応答パターンに酷似していることを発見した。「皮質には内部状態のレパートリがあらかじめ存在し、外部刺激はそれを微調節するか、規定の内部レパートリから特定の状態を選択する」(池谷 2005)。

drpnn でこれを検証した:
1. 固定パターン 10,000 trial 訓練
2. 訓練後、**外部刺激ゼロ** で 10,000 step 走らせ、自発活動を記録
3. 各 300ms window の出力 fingerprint を計算
4. 各刺激応答 fingerprint との cosine 類似度を比較
5. 「空間保存シャッフル」(各ニューロンの総発火回数を保持し、時間 bin だけランダム化) で帰無分布生成

結果:
- **Welch t = 4.169, p < 0.001**: 自発活動の時間構造が刺激応答パターンと有意に類似
- 状態遷移率 **72.4%**: 「数百ミリ秒おきに自然に推移」(Kenet 2003) と整合
- 全 5 パターン (A, B, C, D, E) を実際に遷移
- リミットサイクル (1 状態固定) ではない、真の多重アトラクタ

これは **「皮質の自発活動は単なるノイズではない」** という池谷氏の主張を、確率なしの整数決定論で定量再現したことになる。

整数決定論モデルでこの現象が観察できるという事実は、神経科学的にも興味深い (はずだ)。

## Fork A→F の進化

M1 が現状の姿に到達するまで、6 つのフォーク (A, B, C, D, E, F) を経た。各フォーク 1-10 万 trial の実験で性能を測定:

| Fork | 主な変更 | 結果 |
|---|---|---|
| baseline | 最初の実装 (固定/可塑性 区別あり) | POST=0.41 (旧評価) |
| A | MIN_CONDUCTANCE 20→40 | わずかな改善 |
| B | 新生結線の decay 保護期間 | わずかな改善 |
| C | Fork B + MIN_CONDUCTANCE=40 | 改善せず |
| D | 全シナプス初期 conductance=50 | 改善せず |
| E | 過剰接続 (12,800 plastic) | 改善あり |
| **F** | **vitality + LTP only + デール原則 + 過剰接続** | **POST=0.794 (新評価)** |
| F-G1-R1 | enthalpy 統合 + LTD 復活 | F より高い識別性 |
| F-G1-R1 v2 | 1セル1ニューロン + 8近傍 + UP/DOWN | **POST=0.795** (現時点最高) |

「失敗から学ぶ」が本当に効いたのは Fork E → F の転換点。「**シナプスを fixed/plastic で分けるのは過剰設計**」「**全シナプスを同じ可塑性で扱う**」という気づきが、設計を根本的にシンプル化した。

drpnn の現状は、6 原理 (局所性、物理性、決定論性、整数演算、創発性、平衡としての学習) に最大限忠実な実装に到達している。

## 数値のまとめ

| 指標 | 値 |
|---|---|
| ニューロン数 (M1) | 440 (入力 20 + 興奮 304 + 抑制 76 + 出力 40) |
| シナプス数 (初期) | ~17,000 |
| メモリフットプリント | 約 700 KB |
| 固定パターン POST selectivity | **0.795** (within 0.953, between 0.158) |
| Kenet 2003 検定 | **t = 4.169, p < 0.001** |
| 状態遷移率 | 72.4% (生物観察値 80% に近い) |
| 訓練時間 (10k trial) | 約 30 分 (Ryzen 7、rayon 並列化) |

このサイズと性能を、整数演算のみで、確率なしで達成している。これは **Renesas DRP** のような特殊チップ上で動かす際に決定的な意味を持つ。

## 次回予告

次回は、これらの実装を **リアルタイムで観察できる可視化ツール** について書く。macroquad (Rust の GPU 描画ライブラリ) で実装した可視化で、スパイク、conductance、新生シナプスが時間とともに変化する様子を直接見ることができる。

## 支援のお願い

drpnn は個人プロジェクトで、CC BY-SA 4.0 で公開している。研究を継続するために、GitHub Sponsors で支援を募集している。

- **GitHub**: https://github.com/vivacchi/drpnn
- **GitHub Sponsors**: https://github.com/sponsors/vivacchi

ここまで読んでくれてありがとう。M1 の構造に興味があれば、GitHub Issues や note のコメント欄でぜひ議論しよう。

---

*この記事は CC BY-SA 4.0 で公開されています。引用・転載・改変自由 (帰属表示 + 同条件継承)。*

*次回予告: 「軸索成長を可視化する — 散逸構造形成のリアルタイム観察」*
