# note 公開用 記事 4 本目 (開発ジャーニー)

下記をそのまま note にコピー&貼り付け推奨。

---

## 推奨設定

### タイトル

```
寸法バグから p<0.001 まで — drpnn 開発で起きた数値が変わる瞬間
```

代替案:
- 「自分のコードを疑う技術 — 個人研究者の品質保証ストーリー」
- 「失敗を消さない哲学 — drpnn の Tier 1-3 修正ジャーニー」

### 想定読者
- 1, 2, 3 本目を読んだ人
- ソフトウェア開発者・研究者全般
- 失敗から学ぶ姿勢に共感する人

### 読了時間
8-12 分

### タグ
```
研究
AI
SNN
ソフトウェアテスト
品質保証
Rust
ニューラルネットワーク
オープンソース
DRPNN
開発ジャーニー
```

### 価格
**無料**

---

## 記事本文 (コピペ用)

# 寸法バグから p<0.001 まで — drpnn 開発で起きた数値が変わる瞬間

私の個人研究プロジェクト「**drpnn**」では、2 週間で多くの実装と評価を進めてきた。
[1 本目](https://note.com/...) で全体像、[2 本目](https://note.com/...) で熱力学的な学習の枠組み、[3 本目](https://note.com/...) で M1 一次聴覚野の詳細を紹介した。

今回は、開発の中で実際に起きた **「数値が変わる瞬間」** について書く。失敗を隠さず、修正の過程を全部見せる回。

---

## 発端 — 「t=1.614」の解釈に迷う

2026 年 5 月、私は **池谷裕二教授 (東大薬学) の 2005 年の主張** 「自発活動は単なるノイズではない」を、drpnn で再現できないかを試していた。

具体的には、訓練後のネットワークを **外部刺激ゼロで走らせ**、出力ニューロンの自発活動が、訓練した刺激パターンと類似するかを Welch t-test で検定する。

最初の結果:
```
Welch t-statistic: t = 1.614
判定: 「弱い傾向、有意ではない」
```

p < 0.05 (t > 2) には届かない。「方向性は正しいが実証されない」と判断した。

PAPER に「仮説、現状未実証」と書いて公開した。

---

## 一晩寝かせて、ふと気づく

別の作業をしている時、ふと **自分のコード** が気になった。

```rust
// 実装:
pub fn cosine_similarity(a: &[f64], b: &[f64]) -> f64 {
    let dot: f64 = a.iter().zip(b.iter()).map(|(x, y)| x * y).sum();
    // ...
}
```

`zip` で 2 つのベクトルを並べて要素積を取っている。これは **両者の寸法が同じ前提** だ。

確認すると:
- 刺激応答 fingerprint: 12,600 次元 (内部 420 ニューロン × 30 時間 bin)
- 自発活動 fingerprint: **4,200 次元** (内部 420 ニューロン × 10 時間 bin)

**寸法が違う**。

Rust の `zip` は **短い方に揃えて切り捨てる** ので、panic は起きない。しかしこれは「**fingerprint の前 1/3 だけで比較していた**」ということ。

つまり t=1.614 という数値は、 **意味のある検定の結果ではなかった**。

---

## 修正してみる

```rust
// 修正 1: window 長を統一
const SPONTANEOUS_WINDOW_STEPS: usize = 600;  // 300 ms (trial と同じ)

// 修正 2: cosine_similarity に寸法チェック追加
pub fn cosine_similarity(a: &[f64], b: &[f64]) -> f64 {
    assert_eq!(a.len(), b.len(), "dimension mismatch");
    // ...
}
```

再実行:

```
Welch t-statistic: t = 4.169
判定: 「強く有意 (p < 0.001)」
→ Kenet 2003 内部状態レパートリを定量的に実証 ✓
```

**4 倍以上の t 値**。p 値は `<10⁻⁹` レベル。

---

## 何が起きたか

旧結果 t=1.614 は、 fingerprint の **前 1/3 だけで比較** していた。これは:
- 内部の自発活動が「最初の 100ms」だけに集中していた場合は、まだ意味のある類似度を出せた
- ただし、活動は 300ms 全体に分散していた → 前 1/3 で測ると活動の 2/3 を見逃していた
- 結果として「自発活動と刺激応答パターンが似ている」シグナルが弱まっていた

正しい寸法で全体を比較した瞬間、**強く有意なシグナル** が浮かび上がった。

これは drpnn の中で **「皮質の自発活動は意味のあるパターンを内蔵している」** という、池谷氏の主張を整数決定論版で実証する結果だった。

---

## 「自分のコードを疑う」技術

このバグ修正の経験から、いくつか学んだ:

### 1. silent error は最も危険

Rust の `zip` は便利だが、寸法不一致を黙って受け入れる。これは **safety guarantee なしの API**。 個人開発者が「使い慣れた言語の落とし穴」を踏むと、 結果が静かに歪む。

### 2. 数値の「意味」を毎回確認する

「t=1.614 だから有意ではない」と数値を字面で解釈するのは危険。 **「この t 値はどう計算されたか」「比較しているベクトルは本当に同じものか」** を毎回問い直す必要がある。

### 3. テストは仕様変更で壊れる

drpnn は半年で複数回のフォーク (Fork A → B → C → D → E → F → F-G1-R1) を経た。 そのたびに `ThermoNeuron` の物理が変わり、 古いテストが破綻していた。

これは典型的な「**過去の自分が書いたテストが、現在の自分の仮定を裏切る**」状況。

---

## Tier 1-3 のシステマティック品質保証

寸法バグの発見をきっかけに、 私はコードベース全体を **6 つの設計原理** に照らして系統的に再検証した。

### Tier 1 (重大、即修正):
1. **cosine_similarity に寸法 assert 追加**: 将来同じバグが silent に起きないように
2. **古い時間平滑化 fingerprint を時間 bin 化に統一**: 評価指標の一貫性
3. **G-1 統合で破綻したテストを修正**: テストを現状実装に合わせて更新

### Tier 2 (構造整理):
4. **デッドフィールド削除**: G-1 で機構撤廃した `refractory_remaining` を構造体からも除去
5. **決定論性検証コード整合**: 削除したフィールドの比較を spike_trace 比較に置換
6. **ドキストリング刷新**: 「LTD 削除」と書いていたが実装は LTD あり (R1 復活)、矛盾を解消

### Tier 3 (品質向上):
7. **Welch-Satterthwaite 自由度**: 厳密な自由度計算
8. **正規近似 p 値**: 「t 値だけ表示」から「t, df, p 値」表示へ
9. **multi-seed 対応**: CLI 引数で seed 指定可能、 multi-seed 検証の準備

修正後の検証:
- 全 37 テスト合格
- 1000 trial 決定論性検証で直列版と並列版が完全一致
- 100 trial サニティチェックで POST=0.769 (修正前後で同一値)
- **決定論性が完璧に保持されている**

---

## 影響範囲の正直な分析

このバグ修正で影響を受けたのは、 過去の実験結果のうち **何か** ?

| 過去実験 | 寸法バグの影響 | 信頼性 |
|---|---|---|
| 時間 bin 化評価 (POST 0.756) | なし | ✓ |
| 仮想 M0 (POST 0.795) | なし | ✓ |
| 音素評価 (POST 0.389) | なし | ✓ |
| **旧 Kenet 検証 (t=1.614)** | **あり** | **✗** |
| **修正後 Kenet 検証 (t=4.169)** | なし | ✓ |

影響は **§5.12.3 暫定数値だけ** に限定された。 主要成果 (POST 0.795, Kenet t=4.169) は全て正しい。

PAPER §5.12.3 は「仮説、未実証」から「実証」に書き換え、 修正履歴を全部記録した。

---

## 失敗を消さない哲学

drpnn の PAPER には、 過去の **13 回の R-STDP 報酬学習失敗** も全部記録している。これは設計判断:

> 失敗を消さない理由 — 同じ失敗を繰り返さないため、 そして「なぜこの設計に至ったか」の歴史を残すため

寸法バグの発見と修正も、 同じ哲学で扱う。 これは「**個人プロジェクトだからこそ、開発過程を完全に公開できる**」という強みでもある。

巨大企業のプロダクト開発では、 こうした「失敗からの学び」は内部に閉じる。 オープンソース個人プロジェクトでは、 全部見せられる。 GitHub の commit 履歴に時系列で残っている (`d918cad → 4376347 → 686ff14` あたりが該当)。

---

## ソフトウェアと科学の交差点

私は元々ソフトウェアエンジニアだ。 神経科学者ではない。 しかし、 drpnn を作る中で、 **「コードのバグ」と「科学的仮説の検証」** が深く絡んでいることに気づいた。

- バグがあると、 仮説が正しくても数値が出ない
- 仮説が間違っていると、 コードが正しくても数値が出ない
- そして、 そのどちらが原因かを区別するのは難しい

「t=1.614」が「弱い傾向、未実証」だったのか、 「実は強い実証だがバグで隠れていた」のか、 これを区別する **しかない方法** は、 コードを 1 行ずつ追って確認することだ。

これは大企業の量産研究では時間がかかりすぎて回らない。 **個人プロジェクトの利点は、 全コードを把握できる規模である** ことかもしれない。

---

## 次回予告と支援のお願い

drpnn は個人プロジェクトで、 CC BY-SA 4.0 で全コード・全論文を公開している。

- **GitHub**: https://github.com/vivacchi/drpnn
- **GitHub Sponsors (継続支援)**: https://github.com/sponsors/vivacchi

寸法バグの発見と修正、 Tier 1-3 のシステマティック品質保証 — これらが「個人研究の説得力」を支える基盤だと信じている。

次回は **M2 二次聴覚野 (A2) の設計と実装** について書く予定。M1 から M2 への階層的処理が、 不変性 (invariance) をどう物理プロセスとして実装するか、 という話。

ここまで読んでくれてありがとう。

---

*この記事は CC BY-SA 4.0 で公開されています。引用・転載・改変自由 (帰属表示 + 同条件継承)。*

*次回予告: 「M2 二次聴覚野 — 不変性を物理プロセスで作る」*
